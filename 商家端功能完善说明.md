# 商家端功能完善说明文档

## 功能概述

本次更新完善了商家端的核心功能，包括数据概览、商品管理、订单管理和评论管理等模块。

## 一、数据库更新

### 1.1 新增字段

执行 `database_complete_fix.sql` 脚本，新增以下字段：

**commodity 表**
- `sold` - 已售数量（用于统计商品销量）

**comment 表**
- `merchant_reply` - 商家回复内容
- `merchant_reply_time` - 商家回复时间

**orders 表**
- `original_amount` - 原始金额（改价前）
- `price_modified` - 是否已改价标识

## 二、后端功能实现

### 2.1 商家端数据概览

**接口**: `GET /merchant/dashboard`

**功能**: 获取商家端数据概览，包括：
- 今日订单数和销售额
- 待处理订单数（待支付）
- 待发货订单数
- 商品统计（总数、上架数、下架数）
- 总销售额和总订单数
- 待评价订单数
- 评论统计（总数、待回复数）

**返回数据结构**:
```json
{
  "code": 200,
  "data": {
    "todayOrderCount": 10,
    "todaySales": 1500.00,
    "pendingOrderCount": 5,
    "toShipOrderCount": 8,
    "totalProductCount": 50,
    "onSaleProductCount": 45,
    "offSaleProductCount": 5,
    "totalSales": 50000.00,
    "totalOrderCount": 200,
    "toCommentOrderCount": 15,
    "totalCommentCount": 100,
    "toReplyCommentCount": 10
  }
}
```

### 2.2 商品上下架管理

**接口**: `POST /merchant/products/{productId}/status?isValid=true`

**功能**: 更新商品的上下架状态

**参数**:
- `productId` - 商品ID（路径参数）
- `isValid` - 是否上架（true-上架，false-下架）

**示例**:
```javascript
// 上架商品
updateProductStatus(1, true)

// 下架商品
updateProductStatus(1, false)
```

### 2.3 订单管理

#### 2.3.1 订单列表查询

**接口**: `GET /merchant/orders?status=pending&page=1&size=10`

**功能**: 分页查询订单列表，支持按状态筛选

**参数**:
- `status` - 订单状态（可选）：pending/paid/shipped/completed/cancelled
- `page` - 页码（默认1）
- `size` - 每页大小（默认10）

#### 2.3.2 订单详情查询

**接口**: `GET /merchant/orders/{orderId}`

**功能**: 获取订单详细信息，包括用户信息和订单项

**返回数据包含**:
- 订单基本信息
- 用户信息（姓名、电话）
- 订单项列表
- 改价信息（如有）
- 物流信息（如有）

#### 2.3.3 待支付订单改价

**接口**: `POST /merchant/orders/update-price`

**功能**: 修改待支付订单的价格

**请求体**:
```json
{
  "orderId": 1,
  "newTotalAmount": 100.00,
  "newPostage": 10.00,
  "reason": "优惠活动"
}
```

**业务规则**:
- 只能修改待支付（pending）状态的订单
- 自动保存原始金额
- 改价原因会添加到订单备注中

#### 2.3.4 订单发货

**接口**: `POST /merchant/orders/ship`

**功能**: 为已付款订单填写物流信息并发货

**请求体**:
```json
{
  "orderId": 1,
  "shippingCompany": "顺丰速运",
  "trackingNo": "SF1234567890",
  "remark": "已发货，请注意查收"
}
```

**业务规则**:
- 只能对已付款（paid）状态的订单发货
- 发货后订单状态变更为 shipped
- 自动更新商品销量

### 2.4 评论管理

#### 2.4.1 获取商品评论列表

**接口**: `GET /merchant/products/{productId}/comments?page=1&size=10`

**功能**: 分页查询商品的评论列表

#### 2.4.2 商家回复评论

**接口**: `POST /merchant/comments/reply`

**功能**: 商家回复用户评论

**请求体**:
```json
{
  "commentId": 1,
  "reply": "感谢您的评价，我们会继续努力！"
}
```

#### 2.4.3 删除评论

**接口**: `DELETE /merchant/comments/{commentId}`

**功能**: 删除不当评论（软删除，状态改为deleted）

#### 2.4.4 置顶评论

**接口**: `POST /merchant/comments/{commentId}/top?isTop=true`

**功能**: 置顶或取消置顶评论

## 三、客户端评论功能

### 3.1 创建评论

**接口**: `POST /api/comments/create`

**功能**: 用户对已完成订单的商品进行评论

**请求体**:
```json
{
  "commodityId": 1,
  "orderId": 100,
  "parentId": null,
  "star": 5,
  "content": "商品质量很好，物流也很快！",
  "images": [
    "https://example.com/image1.jpg",
    "https://example.com/image2.jpg"
  ]
}
```

**业务规则**:
- 需要登录
- 只能评论已完成的订单
- 每个商品只能评论一次
- 支持上传评论图片
- 支持回复评论（设置parentId）

### 3.2 查看评论

**接口**: `GET /api/comments/page?commodityId=1&current=1&size=10`

**功能**: 分页查询商品评论，包含用户信息和图片

## 四、前端API接口

### 4.1 商家端API（merchant.js）

```javascript
// 数据概览
getDashboard()

// 订单管理
getOrderList({ status: 'pending', page: 1, size: 10 })
getOrderDetail(orderId)
updateOrderPrice({ orderId, newTotalAmount, newPostage, reason })
shipOrder({ orderId, shippingCompany, trackingNo, remark })

// 商品管理
updateProductStatus(productId, isValid)

// 评论管理
getProductComments(productId, { page: 1, size: 10 })
replyComment({ commentId, reply })
deleteComment(commentId)
topComment(commentId, isTop)
```

### 4.2 客户端API（comment.js）

```javascript
// 查看评论
getCommentsByPage(commodityId, current, size)
getCommentCount(commodityId)

// 创建评论
createComment({
  commodityId,
  orderId,
  star,
  content,
  images
})
```

## 五、使用示例

### 5.1 商家端数据概览

```javascript
import { getDashboard } from '@/api/merchant'

// 获取数据概览
const dashboard = await getDashboard()
console.log('今日订单数:', dashboard.todayOrderCount)
console.log('今日销售额:', dashboard.todaySales)
```

### 5.2 商品上下架

```javascript
import { updateProductStatus } from '@/api/merchant'

// 上架商品
await updateProductStatus(1, true)

// 下架商品
await updateProductStatus(1, false)
```

### 5.3 订单改价

```javascript
import { updateOrderPrice } from '@/api/merchant'

await updateOrderPrice({
  orderId: 1,
  newTotalAmount: 100.00,
  newPostage: 10.00,
  reason: '优惠活动'
})
```

### 5.4 订单发货

```javascript
import { shipOrder } from '@/api/merchant'

await shipOrder({
  orderId: 1,
  shippingCompany: '顺丰速运',
  trackingNo: 'SF1234567890',
  remark: '已发货，请注意查收'
})
```

### 5.5 商家回复评论

```javascript
import { replyComment } from '@/api/merchant'

await replyComment({
  commentId: 1,
  reply: '感谢您的评价，我们会继续努力！'
})
```

### 5.6 客户端评论

```javascript
import { createComment } from '@/api/comment'

await createComment({
  commodityId: 1,
  orderId: 100,
  star: 5,
  content: '商品质量很好，物流也很快！',
  images: [
    'https://example.com/image1.jpg',
    'https://example.com/image2.jpg'
  ]
})
```

## 六、注意事项

1. **权限控制**: 商家端接口需要商家登录后才能访问，需要在请求头中携带商家Token
2. **订单状态**: 改价只能对待支付订单，发货只能对已付款订单
3. **评论权限**: 用户只能评论自己已完成的订单
4. **销量统计**: 发货时自动更新商品销量
5. **软删除**: 评论删除采用软删除方式，不会真正删除数据

## 七、数据库执行

在使用新功能前，请先执行数据库更新脚本：

```bash
mysql -u root -p petshop < database_complete_fix.sql
```

## 八、测试建议

1. 测试数据概览接口，确认统计数据正确
2. 测试商品上下架功能
3. 测试订单改价功能（待支付订单）
4. 测试订单发货功能（已付款订单）
5. 测试客户端评论功能
6. 测试商家端评论管理功能

## 九、后续优化建议

1. 添加订单批量发货功能
2. 添加评论审核功能
3. 添加数据导出功能
4. 添加销售报表功能
5. 添加库存预警功能
