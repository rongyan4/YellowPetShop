# 订单确认和支付系统功能说明

## 功能概述

实现了完整的订单确认和支付流程，包括：
- 订单确认页面（地址选择、商品列表、数量修改、支付方式选择）
- 钱包支付功能（自定义数字键盘、密码输入、余额检查）
- 支付密码验证（3次错误锁定30分钟）
- 地址管理功能

---

## 数据库变更

### 执行脚本

```bash
mysql -u root -p petshop < payment_system.sql
```

### 新增表结构

#### 1. user_address（用户地址表）
```sql
CREATE TABLE `user_address` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `receiver_name` varchar(100) NOT NULL,
  `receiver_phone` varchar(20) NOT NULL,
  `province` varchar(50) NOT NULL,
  `city` varchar(50) NOT NULL,
  `district` varchar(50) NOT NULL,
  `detail_address` varchar(500) NOT NULL,
  `is_default` tinyint(1) NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`)
);
```

#### 2. user_wallet（用户钱包表）
```sql
CREATE TABLE `user_wallet` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `balance` decimal(10, 2) NOT NULL DEFAULT 0.00,
  `pay_password` varchar(255) NULL COMMENT '支付密码（BCrypt加密）',
  `is_locked` tinyint(1) NOT NULL DEFAULT 0,
  `lock_time` datetime NULL,
  `error_count` int NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `uk_user_id`(`user_id`)
);
```

#### 3. payment_record（支付记录表）
```sql
CREATE TABLE `payment_record` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `order_id` bigint NOT NULL,
  `user_id` bigint NOT NULL,
  `payment_method` varchar(20) NOT NULL,
  `amount` decimal(10, 2) NOT NULL,
  `status` varchar(20) NOT NULL DEFAULT 'PENDING',
  `transaction_no` varchar(100) NULL,
  `pay_time` datetime NULL,
  PRIMARY KEY (`id`)
);
```

#### 4. wallet_transaction（钱包流水表）
```sql
CREATE TABLE `wallet_transaction` (
  `id` bigint NOT NULL AUTO_INCREMENT,
  `user_id` bigint NOT NULL,
  `order_id` bigint NULL,
  `type` varchar(20) NOT NULL COMMENT 'RECHARGE-充值 PAYMENT-支付 REFUND-退款',
  `amount` decimal(10, 2) NOT NULL,
  `balance_before` decimal(10, 2) NOT NULL,
  `balance_after` decimal(10, 2) NOT NULL,
  `description` varchar(255) NULL,
  PRIMARY KEY (`id`)
);
```

#### 5. 修改 orders 表
```sql
ALTER TABLE `orders` 
ADD COLUMN `payment_method` varchar(20) NULL COMMENT '支付方式';
```

### 测试数据

脚本已包含测试数据：
- 用户ID=1 的两个地址
- 用户ID=1 的钱包（余额10000元，支付密码：123456）

---

## 后端接口

### 地址管理接口

#### 1. 获取用户地址列表
- **接口**: `GET /api/address/list`
- **需要登录**: 是
- **返回**: 地址列表（默认地址排在前面）

#### 2. 获取默认地址
- **接口**: `GET /api/address/default`
- **需要登录**: 是
- **返回**: 默认地址信息

#### 3. 添加地址
- **接口**: `POST /api/address/add`
- **需要登录**: 是
- **请求体**:
```json
{
  "receiverName": "张三",
  "receiverPhone": "13800138000",
  "province": "北京市",
  "city": "北京市",
  "district": "朝阳区",
  "detailAddress": "建国路88号",
  "isDefault": true
}
```

#### 4. 设置默认地址
- **接口**: `PUT /api/address/setDefault/{addressId}`
- **需要登录**: 是

### 支付相关接口

#### 1. 获取钱包信息
- **接口**: `GET /api/payment/wallet`
- **需要登录**: 是
- **返回**:
```json
{
  "id": 1,
  "userId": 1,
  "balance": 10000.00,
  "isLocked": false,
  "hasPayPassword": true
}
```

#### 2. 订单预览（从购物车）
- **接口**: `POST /api/payment/preview/cart`
- **需要登录**: 是
- **请求体**: `[1, 2, 3]` (购物车商品ID列表)
- **返回**: 订单预览信息

#### 3. 订单预览（立即购买）
- **接口**: `GET /api/payment/preview/direct?commodityId=1&quantity=2`
- **需要登录**: 是
- **返回**: 订单预览信息

#### 4. 创建订单并支付
- **接口**: `POST /api/payment/createAndPay`
- **需要登录**: 是
- **请求体**:
```json
{
  "items": [
    {
      "commodityId": 1,
      "quantity": 2
    }
  ],
  "addressId": 1,
  "paymentMethod": "WALLET",
  "remark": "请尽快发货"
}
```

#### 5. 验证支付密码
- **接口**: `POST /api/payment/verifyPassword?payPassword=123456`
- **需要登录**: 是
- **返回**: true/false
- **错误处理**: 
  - 密码错误会增加错误次数
  - 3次错误后锁定30分钟

---

## 前端实现

### 页面结构

#### OrderConfirmView.vue（订单确认页）

**主要功能**:
1. **地址选择**
   - 显示默认地址
   - 点击可选择其他地址
   - 地址列表弹窗

2. **商品列表**
   - 显示商品信息
   - 支持修改数量（van-stepper）
   - 实时计算金额

3. **支付方式选择**
   - 钱包支付（显示余额）
   - 微信支付（暂不支持）
   - 支付宝支付（暂不支持）

4. **订单备注**
   - 可选填写
   - 最多200字

5. **金额显示**
   - 商品金额
   - 运费
   - 实付款

6. **支付密码输入**
   - 自定义数字键盘
   - 6位密码输入
   - 圆点显示
   - 错误提示
   - 3次错误锁定

### 路由跳转

#### 从购物车结算
```javascript
router.push({
  path: '/order/confirm',
  query: {
    source: 'cart',
    cartItemIds: JSON.stringify([1, 2, 3])
  }
});
```

#### 从商品详情立即购买
```javascript
router.push({
  path: '/order/confirm',
  query: {
    source: 'direct',
    commodityId: 1,
    quantity: 1
  }
});
```

### 支付流程

```
1. 用户点击"提交订单"
   ↓
2. 验证地址、商品、支付方式
   ↓
3. 如果选择钱包支付
   ├─ 检查钱包状态（是否锁定）
   ├─ 检查是否设置支付密码
   ├─ 检查余额是否充足
   └─ 显示支付密码输入弹窗
   ↓
4. 用户输入6位密码
   ↓
5. 自动验证密码
   ├─ 正确：创建订单并扣款
   └─ 错误：提示错误次数，清空密码
   ↓
6. 支付成功
   ├─ 更新订单状态为"已付款"
   ├─ 扣除钱包余额
   └─ 跳转到订单列表
```

### 自定义数字键盘

```javascript
const keyboard = [
  ['1', '2', '3'],
  ['4', '5', '6'],
  ['7', '8', '9'],
  ['', '0', 'delete']
];

const handleKeyPress = (key) => {
  if (key === 'delete') {
    password.value = password.value.slice(0, -1);
  } else if (key !== '') {
    if (password.value.length < 6) {
      password.value += key;
      
      // 输入完成后自动验证
      if (password.value.length === 6) {
        verifyPassword();
      }
    }
  }
};
```

### 密码输入框

```html
<div class="password-input">
  <div 
    v-for="i in 6" 
    :key="i" 
    class="password-box"
    :class="{ active: password.length === i - 1 }"
  >
    <span v-if="password.length >= i" class="dot">●</span>
  </div>
</div>
```

---

## 支付方式说明

### 1. 钱包支付（已实现）

**流程**:
1. 验证支付密码
2. 检查余额
3. 扣款
4. 更新订单状态

**安全机制**:
- 支付密码使用 BCrypt 加密存储
- 密码错误3次锁定30分钟
- 锁定期间无法支付

### 2. 微信支付（预留）

**提示**: "暂不支持微信支付"

**预留接口**:
- 后端返回错误信息
- 前端显示提示

### 3. 支付宝支付（预留）

**提示**: "暂不支持支付宝支付"

**预留接口**:
- 后端返回错误信息
- 前端显示提示

---

## 安全机制

### 1. 支付密码保护

- **加密存储**: 使用 BCrypt 加密
- **错误次数限制**: 3次错误锁定
- **锁定时长**: 30分钟
- **自动解锁**: 超时后自动解锁

### 2. 余额检查

- 支付前检查余额是否充足
- 余额不足时提示用户

### 3. 订单验证

- 验证商品是否存在
- 验证商品是否有效
- 验证地址是否属于用户
- 验证订单金额

---

## 使用说明

### 1. 执行数据库脚本

```bash
mysql -u root -p petshop < payment_system.sql
```

### 2. 重启后端服务

确保新的 Mapper 和 Service 被加载。

### 3. 测试流程

#### 测试账号
- 用户ID: 1
- 钱包余额: 10000元
- 支付密码: 123456

#### 测试步骤

1. **登录账号**

2. **添加商品到购物车**
   - 进入商品详情页
   - 点击"加入购物车"

3. **购物车结算**
   - 进入购物车
   - 选中商品
   - 点击"结算"
   - 进入订单确认页

4. **或立即购买**
   - 在商品详情页
   - 点击"立即购买"
   - 进入订单确认页

5. **订单确认页操作**
   - 查看/选择收货地址
   - 修改商品数量
   - 选择支付方式（钱包）
   - 填写备注（可选）
   - 点击"提交订单"

6. **支付密码输入**
   - 弹出支付窗口
   - 查看钱包余额和支付金额
   - 使用数字键盘输入密码: 123456
   - 自动验证并支付

7. **支付成功**
   - 显示"支付成功"提示
   - 跳转到订单列表

### 4. 测试密码错误

1. 输入错误密码（如：111111）
2. 提示"支付密码错误，还可尝试2次"
3. 再次输入错误密码
4. 提示"支付密码错误，还可尝试1次"
5. 第三次输入错误
6. 提示"密码错误次数过多，钱包已锁定30分钟"
7. 钱包被锁定，无法支付

---

## API 文件

### payment.js
```javascript
- getWalletInfo() // 获取钱包信息
- previewOrderFromCart() // 订单预览（购物车）
- previewOrderFromDirect() // 订单预览（立即购买）
- createAndPayOrder() // 创建订单并支付
- verifyPayPassword() // 验证支付密码
```

### address.js
```javascript
- getAddressList() // 获取地址列表
- getDefaultAddress() // 获取默认地址
- addAddress() // 添加地址
- setDefaultAddress() // 设置默认地址
```

---

## 文件清单

### 数据库脚本
- `payment_system.sql` - 支付系统表结构和测试数据

### 后端文件

**实体类**:
- `UserWallet.java` - 钱包实体
- `UserAddress.java` - 地址实体
- `OrderConfirmDTO.java` - 订单确认DTO
- `OrderPreviewVO.java` - 订单预览VO
- `PaymentDTO.java` - 支付DTO
- `WalletVO.java` - 钱包VO

**Mapper**:
- `UserWalletMapper.java` - 钱包Mapper
- `UserAddressMapper.java` - 地址Mapper

**Service**:
- `PaymentService.java` - 支付服务接口
- `PaymentServiceImpl.java` - 支付服务实现
- `AddressService.java` - 地址服务接口
- `AddressServiceImpl.java` - 地址服务实现

**Controller**:
- `PaymentController.java` - 支付控制器
- `AddressController.java` - 地址控制器

### 前端文件

**API**:
- `payment.js` - 支付相关API
- `address.js` - 地址相关API

**页面**:
- `OrderConfirmView.vue` - 订单确认页（新增）

**修改的页面**:
- `CarView.vue` - 购物车页（修改结算逻辑）
- `GoodDetailsView.vue` - 商品详情页（修改立即购买逻辑）

---

## 注意事项

1. **支付密码**: 测试数据中的支付密码是 `123456`，已使用 BCrypt 加密

2. **钱包余额**: 测试账号余额为 10000 元，可以正常支付

3. **地址数据**: 已预置两个测试地址，默认地址会自动选中

4. **路由配置**: 需要在路由文件中添加 `/order/confirm` 路由

5. **锁定机制**: 密码错误3次后锁定30分钟，可以通过修改数据库解锁：
```sql
UPDATE user_wallet SET is_locked = 0, error_count = 0, lock_time = NULL WHERE user_id = 1;
```

---

## 后续优化建议

1. **地址管理完善**
   - 编辑地址
   - 删除地址
   - 地址验证

2. **支付密码管理**
   - 设置支付密码
   - 修改支付密码
   - 忘记密码找回

3. **钱包功能**
   - 充值功能
   - 流水查询
   - 余额明细

4. **支付方式扩展**
   - 接入微信支付
   - 接入支付宝支付
   - 银行卡支付

5. **订单优化**
   - 优惠券功能
   - 积分抵扣
   - 运费计算优化

6. **用户体验**
   - 指纹/面容支付
   - 记住支付密码（限时）
   - 支付成功动画
