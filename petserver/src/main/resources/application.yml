# ????
spring:
  application:
    name: petserver

  # ???????????????????????????
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/pet_shop?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf-8}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10 # ????????
      connection-timeout: 30000 # ???????30??

  # MyBatis-Plus ??
  mybatis-plus:
    type-aliases-package: com.yellow.petshop.model # ??????
    configuration:
      map-underscore-to-camel-case: true # ??????
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ??SQL??
    global-config:
      db-config:
        id-type: AUTO # ????

  # ??????
  mvc:
    static-path-pattern: /images/** # ??????
  web:
    resources:
      static-locations: classpath:/static/ # ??????

# ?????
server:
  port: 3000 # ??2
  address: 0.0.0.0 # ?????????????

# JWT???????Token???
#jwt:
#  secret: ${JWT_SECRET:your-secret-key-32bytes-long-12345678} # 32?????
#  expiration: 7200000 # Token?????2?????????

springdoc:
  swagger-ui:
    path: /swagger-ui.html  # Swagger??????????/swagger-ui.html?
    operationsSorter: method  # ????????HTTP?????GET/POST/PUT/DELETE?
  api-docs:
    path: /v3/api-docs  # ????JSON?????????????
  packages-to-scan: com.yellow.petshop.controller  # ???Controller?????????????

# ?????????????????????????
openapi:
  info:
    title: ????API??  # ????
    description: ???????????????????  # ????
    version: 1.0.0  # ????
    contact:
      name: rongyan  # ???
      email: 2603726252@qq.com  # ????