# 商品管理和订单管理功能修复说明

## 修复日期
2026年2月20日

## 问题描述

### 1. 订单管理接口404问题
- **原因**：`MerchantController` 缺少 `/api` 前缀，导致前端请求 `/api/merchant/orders` 时找不到对应的后端接口
- **影响**：商家端无法访问订单列表、订单详情等功能

### 2. 商品管理功能不完整
- **原因**：前端调用的API路径与后端不匹配，且 `MerchantController` 中缺少商品管理接口
- **影响**：商家端无法查看、添加、编辑、删除商品

## 修复内容

### 后端修改

#### 1. MerchantController.java
**文件路径**：`petserver/src/main/java/com/yellow/petshop/controller/MerchantController.java`

**修改内容**：
- 添加 `/api` 前缀到 `@RequestMapping`
  ```java
  @RequestMapping("/api/merchant")  // 原来是 "/merchant"
  ```

- 在 `MerchantController` 中添加了商品管理接口（已有独立的 `MerchantGoodsController`，这些接口作为备用）：
  - `GET /api/merchant/products` - 分页查询商品列表
  - `POST /api/merchant/products` - 添加商品
  - `PUT /api/merchant/products/{productId}` - 更新商品
  - `DELETE /api/merchant/products/{productId}` - 删除商品

#### 2. 已存在的 MerchantGoodsController.java
**文件路径**：`petserver/src/main/java/com/yellow/petshop/controller/MerchantGoodsController.java`

**接口列表**：
- `GET /api/merchant/goods/list` - 分页查询商品列表
- `POST /api/merchant/goods/add` - 添加商品
- `PUT /api/merchant/goods/update` - 更新商品
- `DELETE /api/merchant/goods/delete/{id}` - 删除商品
- `DELETE /api/merchant/goods/batch-delete` - 批量删除商品

### 前端修改

#### 1. merchant.js
**文件路径**：`vue-pet/src/api/merchant.js`

**新增API方法**：
```javascript
// 分页查询商品列表
export function getProductList(params)

// 添加商品
export function addProduct(data)

// 更新商品
export function updateProduct(productId, data)

// 删除商品
export function deleteProduct(productId)
```

**API路径**：使用 `MerchantGoodsController` 的路径
- `/merchant/goods/list`
- `/merchant/goods/add`
- `/merchant/goods/update`
- `/merchant/goods/delete/{id}`

#### 2. GoodsManagement.vue
**文件路径**：`vue-pet/src/views/merchant/GoodsManagement.vue`

**修改内容**：
- 实现 `loadGoodsList()` 方法，调用后端API获取商品列表
- 实现 `handleDelete()` 方法，调用后端API删除商品
- 实现 `handleSubmit()` 方法，调用后端API添加/更新商品
- 调整表单字段，移除 `stock` 字段（数据库中没有此字段）
- 修正商品图片字段从 `image` 改为 `mainPicUrl`
- 修正销量字段从 `sales` 改为 `sold`

#### 3. vue.config.js
**文件路径**：`vue-pet/vue.config.js`

**保持不变**：代理配置保持 `/api` 前缀映射
```javascript
pathRewrite: {
  '^/api': '/api'  // 保持 /api 前缀
}
```

## 接口路径对照表

### 订单管理接口
| 功能 | 前端调用路径 | 后端实际路径 | 状态 |
|------|------------|------------|------|
| 获取订单列表 | `/api/merchant/orders` | `@GetMapping("/orders")` in `/api/merchant` | ✅ 已修复 |
| 获取订单详情 | `/api/merchant/orders/{orderId}` | `@GetMapping("/orders/{orderId}")` | ✅ 已修复 |
| 修改订单价格 | `/api/merchant/orders/update-price` | `@PostMapping("/orders/update-price")` | ✅ 已修复 |
| 订单发货 | `/api/merchant/orders/ship` | `@PostMapping("/orders/ship")` | ✅ 已修复 |

### 商品管理接口
| 功能 | 前端调用路径 | 后端实际路径 | 状态 |
|------|------------|------------|------|
| 获取商品列表 | `/api/merchant/goods/list` | `@GetMapping("/list")` in `/api/merchant/goods` | ✅ 已修复 |
| 添加商品 | `/api/merchant/goods/add` | `@PostMapping("/add")` | ✅ 已修复 |
| 更新商品 | `/api/merchant/goods/update` | `@PutMapping("/update")` | ✅ 已修复 |
| 删除商品 | `/api/merchant/goods/delete/{id}` | `@DeleteMapping("/delete/{id}")` | ✅ 已修复 |
| 更新商品状态 | `/api/merchant/products/{productId}/status` | `@PostMapping("/products/{productId}/status")` | ✅ 已有 |

## 测试建议

### 1. 重启服务
修改后需要重启前后端服务：
```bash
# 后端：重新编译并启动
cd petserver
mvn clean package
java -jar target/petserver.jar

# 前端：重启开发服务器
cd vue-pet
npm run serve
```

### 2. 测试订单管理
1. 登录商家后台
2. 访问订单管理页面
3. 测试订单列表加载
4. 测试订单详情查看
5. 测试改价功能（待付款订单）
6. 测试发货功能（已付款订单）

### 3. 测试商品管理
1. 登录商家后台
2. 访问商品管理页面
3. 测试商品列表加载
4. 测试添加商品
5. 测试编辑商品
6. 测试删除商品
7. 测试商品上下架

## 注意事项

1. **数据库字段对应**：
   - 商品表中没有 `stock` 字段，前端已移除
   - 商品图片字段是 `mainPicUrl`，不是 `image`
   - 销量字段是 `sold`，不是 `sales`

2. **API路径统一**：
   - 所有商家端接口都应该有 `/api` 前缀
   - 建议后续统一使用 RESTful 风格的路径

3. **权限验证**：
   - 所有商家端接口都需要验证 JWT Token
   - Token 存储在 `localStorage` 的 `merchant_token` 中

4. **错误处理**：
   - 前端已添加完整的错误提示
   - 后端返回统一的 `Result` 对象

## 后续优化建议

1. **统一接口风格**：
   - 将 `MerchantController` 中的商品管理接口移除，统一使用 `MerchantGoodsController`
   - 或者将 `MerchantGoodsController` 的功能合并到 `MerchantController`

2. **添加更多功能**：
   - 商品批量操作（批量上下架、批量删除）
   - 商品分类管理
   - 商品库存管理
   - 商品图片上传

3. **性能优化**：
   - 添加商品列表缓存
   - 优化分页查询性能
   - 添加搜索索引

4. **用户体验**：
   - 添加商品预览功能
   - 添加商品导入导出功能
   - 添加操作日志查看
