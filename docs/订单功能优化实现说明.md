# 订单功能优化实现说明

## 本次更新内容

### 1. 确认订单页面优化（OrderConfirmView.vue）

#### 1.1 支付方式优化
- ✅ 微信支付和支付宝支付右侧添加单选框（灰色禁用状态）
- ✅ 添加 `.disabled-payment` 样式类，设置透明度和禁用光标
- ✅ 支付图标添加 `.disabled` 类，降低透明度
- ✅ 保留"暂不支持"提示文字

#### 1.2 商品数量调整
- ✅ 已支持使用 `van-stepper` 组件调整商品数量
- ✅ 数量变化时自动重新计算金额

### 2. 订单详情页面（OrderDetailView.vue）- 全新实现

#### 2.1 页面结构
- **订单状态区域**：显示订单当前状态图标、文字和描述
- **订单信息区域**：订单号、创建时间、支付时间、发货时间、完成时间
- **收货信息区域**：收货人姓名、电话、地址（不可修改）
- **商品信息区域**：商品列表，点击可跳转到商品详情页
- **支付方式区域**：
  - 待付款订单：可选择支付方式（钱包支付可用，微信/支付宝灰色禁用）
  - 其他状态订单：支付方式不可修改
- **订单备注区域**：显示订单备注（如果有）
- **金额明细区域**：商品金额、运费、实付款
- **底部操作栏**：待付款订单显示"立即支付"按钮

#### 2.2 功能特性
- ✅ 不可修改商品数量（只显示，无 stepper 组件）
- ✅ 不可修改收货地址（只显示）
- ✅ 待付款订单可选择支付方式并支付
- ✅ 点击商品跳转到商品详情页
- ✅ 支付密码输入和验证
- ✅ 支付成功后刷新订单详情

#### 2.3 状态展示
不同订单状态显示不同的图标和颜色：
- 待付款（PENDING）：时钟图标，橙色
- 待发货（PAID）：对勾图标，蓝色
- 运输中（SHIPPED）：物流图标，紫色
- 已完成（COMPLETED）：成功图标，绿色
- 已取消（CANCELLED）：关闭图标，灰色

### 3. 我的订单页面优化（MyOrdersView.vue）

#### 3.1 功能完善
- ✅ "去支付"按钮跳转到订单详情页（`/order/detail/:orderId`）
- ✅ 点击订单商品区域跳转到订单详情页
- ✅ 订单详情页路由已配置

### 4. 后端接口新增

#### 4.1 PaymentController.java
新增接口：
```java
POST /api/payment/pay
```
- 功能：支付已存在的订单
- 参数：PaymentDTO（orderId, paymentMethod, payPassword）
- 返回：支付结果

#### 4.2 PaymentService.java
新增方法：
```java
Boolean payOrder(Long userId, PaymentDTO paymentDTO)
```

#### 4.3 PaymentServiceImpl.java
实现支付订单逻辑：
- 验证订单存在和权限
- 验证订单状态（只有待付款订单可支付）
- 处理钱包支付（验证密码、检查余额、扣款、更新订单状态）
- 支持微信/支付宝支付（暂时抛出异常）

### 5. 前端 API 更新

#### 5.1 payment.js
新增接口：
```javascript
payOrder(paymentData)
payOrderSafe(paymentData)
```

### 6. 路由配置

新增路由：
```javascript
{
  path: "/order/detail/:orderId",
  name: "order-detail",
  component: () => import("../views/OrderDetailView.vue"),
  meta: { requiresAuth: true }
}
```

## 使用流程

### 场景1：从我的订单支付待付款订单
1. 进入"我的订单"页面
2. 找到待付款订单
3. 点击"去支付"按钮
4. 跳转到订单详情页
5. 选择支付方式（钱包支付）
6. 点击"立即支付"
7. 输入支付密码
8. 支付成功，订单状态更新为"待发货"

### 场景2：查看订单详情
1. 进入"我的订单"页面
2. 点击任意订单的商品区域
3. 跳转到订单详情页
4. 查看订单完整信息
5. 点击商品可跳转到商品详情页

### 场景3：从订单详情跳转到商品详情
1. 在订单详情页
2. 点击任意商品
3. 跳转到商品详情页（`/good?id={commodityId}`）

## 页面对比

| 功能 | 确认订单页 | 订单详情页 |
|------|-----------|-----------|
| 修改商品数量 | ✅ 可以 | ❌ 不可以 |
| 选择收货地址 | ✅ 可以 | ❌ 不可以（只显示） |
| 选择支付方式 | ✅ 可以 | ✅ 待付款订单可以 |
| 订单号显示 | ❌ 无 | ✅ 有 |
| 创建时间显示 | ❌ 无 | ✅ 有 |
| 支付时间显示 | ❌ 无 | ✅ 有（如果已支付） |
| 订单状态显示 | ❌ 无 | ✅ 有 |
| 点击商品跳转 | ❌ 不可以 | ✅ 可以 |

## 样式特点

### 确认订单页
- 可编辑的交互元素（stepper、地址选择）
- 强调操作性

### 订单详情页
- 信息展示为主
- 卡片式布局，清晰分区
- 状态突出显示（大图标+颜色）
- 只读信息使用灰色背景区分

## 测试建议

1. **支付流程测试**
   - 创建待付款订单
   - 从我的订单进入订单详情
   - 选择钱包支付
   - 输入正确/错误密码
   - 验证支付成功后订单状态变化

2. **页面跳转测试**
   - 我的订单 → 订单详情
   - 订单详情 → 商品详情
   - 验证路由参数传递正确

3. **权限测试**
   - 尝试访问其他用户的订单详情
   - 验证权限检查生效

4. **状态显示测试**
   - 手动修改数据库订单状态
   - 验证不同状态的图标和颜色显示正确

## 注意事项

1. 订单详情页的支付方式选择只在待付款状态下可用
2. 微信和支付宝支付方式已设置为灰色禁用状态
3. 支付密码错误3次会锁定钱包30分钟
4. 订单详情页不允许修改商品数量和收货地址
5. 点击商品跳转到商品详情页使用的是 `/good?id={commodityId}` 路由
