# 订单库存和销量管理实现说明

## 功能概述

实现了订单状态变化时的库存和销量自动管理：

1. **用户下单并支付成功时** → 库存 -1
2. **订单完成时（确认收货）** → 销量 +1
3. **待支付订单被取消后** → 库存 +1
4. **订单超时自动取消** → 库存 +1

## 实现细节

### 1. 数据库层 - CommodityMapper.java

新增三个库存和销量管理方法：

```java
/**
 * 减少商品库存（支付成功时调用）
 * 使用乐观锁机制：只有库存充足时才能扣减成功
 */
@Update("UPDATE commodity SET stock = stock - #{quantity} WHERE id = #{commodityId} AND stock >= #{quantity}")
int decreaseStock(@Param("commodityId") Long commodityId, @Param("quantity") Integer quantity);

/**
 * 增加商品库存（取消订单时调用）
 */
@Update("UPDATE commodity SET stock = stock + #{quantity} WHERE id = #{commodityId}")
int increaseStock(@Param("commodityId") Long commodityId, @Param("quantity") Integer quantity);

/**
 * 增加商品销量（确认收货时调用）
 */
@Update("UPDATE commodity SET sold = sold + #{quantity} WHERE id = #{commodityId}")
int increaseSold(@Param("commodityId") Long commodityId, @Param("quantity") Integer quantity);
```

### 2. 业务层 - OrderServiceImpl.java

#### 2.1 支付成功 - 扣减库存

在 `payOrder()` 方法中，支付成功后扣减库存：

```java
// 支付成功后，扣减商品库存
List<OrderItem> items = orderItemMapper.selectByOrderId(orderId);
for (OrderItem item : items) {
    int result = commodityMapper.decreaseStock(item.getCommodityId(), item.getQuantity());
    if (result == 0) {
        // 库存不足，回滚事务
        throw new RuntimeException("商品库存不足：" + item.getCommodityName());
    }
}
```

**特点：**
- 使用 `@Transactional` 保证事务一致性
- 库存不足时自动回滚整个支付流程
- 使用乐观锁防止超卖

#### 2.2 取消订单 - 恢复库存

在 `cancelOrder()` 方法中，取消订单时恢复库存：

```java
// 查询订单商品明细，恢复库存
List<OrderItem> items = orderItemMapper.selectByOrderId(orderId);
for (OrderItem item : items) {
    // 恢复库存
    int result = commodityMapper.increaseStock(item.getCommodityId(), item.getQuantity());
    if (result == 0) {
        throw new RuntimeException("恢复库存失败：" + item.getCommodityName());
    }
}
```

**特点：**
- 只有待支付状态的订单可以取消
- 取消时自动恢复所有商品库存
- 事务保证数据一致性

#### 2.3 确认收货 - 增加销量

在 `confirmReceipt()` 方法中，确认收货时增加销量：

```java
// 查询订单商品明细，增加销量
List<OrderItem> items = orderItemMapper.selectByOrderId(orderId);
for (OrderItem item : items) {
    // 增加销量
    int result = commodityMapper.increaseSold(item.getCommodityId(), item.getQuantity());
    if (result == 0) {
        throw new RuntimeException("更新销量失败：" + item.getCommodityName());
    }
}
```

**特点：**
- 只有已发货状态的订单可以确认收货
- 确认收货后增加商品销量
- 销量用于展示商品热度

### 3. 定时任务 - OrderTimeoutTask.java

超时订单自动取消时恢复库存：

```java
// 查询订单商品明细，恢复库存
List<OrderItem> items = orderItemMapper.selectByOrderId(order.getId());
for (OrderItem item : items) {
    // 恢复库存
    int result = commodityMapper.increaseStock(item.getCommodityId(), item.getQuantity());
    if (result > 0) {
        System.out.println("  - 恢复商品 " + item.getCommodityName() + " 库存 " + item.getQuantity());
    }
}
```

**特点：**
- 每分钟自动检查超时订单
- 超时时间：5分钟
- 自动取消并恢复库存

## 业务流程

### 流程1：正常购买流程

```
1. 用户下单 → 创建订单（状态：PENDING）
2. 用户支付 → 扣减库存（stock - quantity）→ 订单状态变为 PAID
3. 商家发货 → 订单状态变为 SHIPPED
4. 用户确认收货 → 增加销量（sold + quantity）→ 订单状态变为 COMPLETED
```

### 流程2：用户取消订单

```
1. 用户下单 → 创建订单（状态：PENDING）
2. 用户取消 → 恢复库存（stock + quantity）→ 订单状态变为 CANCELLED
```

### 流程3：订单超时自动取消

```
1. 用户下单 → 创建订单（状态：PENDING）
2. 5分钟后未支付 → 定时任务自动取消 → 恢复库存（stock + quantity）→ 订单状态变为 CANCELLED
```

## 数据一致性保证

### 1. 事务管理
- 所有涉及库存变更的操作都使用 `@Transactional`
- 任何步骤失败都会回滚整个事务

### 2. 乐观锁机制
```sql
UPDATE commodity SET stock = stock - #{quantity} 
WHERE id = #{commodityId} AND stock >= #{quantity}
```
- 只有库存充足时才能扣减成功
- 防止超卖问题

### 3. 状态校验
- 支付时检查订单状态必须是 PENDING
- 取消时检查订单状态必须是 PENDING
- 确认收货时检查订单状态必须是 SHIPPED

## 测试场景

### 场景1：正常购买
```
1. 商品库存：100
2. 用户购买：5件
3. 支付成功 → 库存变为：95
4. 确认收货 → 销量增加：5
```

### 场景2：库存不足
```
1. 商品库存：3
2. 用户购买：5件
3. 支付时 → 提示"商品库存不足"
4. 订单保持 PENDING 状态
5. 库存不变：3
```

### 场景3：取消订单
```
1. 商品库存：100
2. 用户下单：5件（未支付）
3. 用户取消 → 库存不变：100（因为未支付，库存未扣减）
```

### 场景4：超时取消
```
1. 商品库存：100
2. 用户下单：5件（未支付）
3. 5分钟后 → 自动取消
4. 库存不变：100（因为未支付，库存未扣减）
```

## 注意事项

1. **库存扣减时机**：支付成功后立即扣减，而不是下单时扣减
2. **销量增加时机**：确认收货后增加，而不是支付后增加
3. **并发控制**：使用乐观锁防止超卖
4. **事务一致性**：所有操作都在事务中执行
5. **日志记录**：关键操作都有日志输出，便于追踪

## 优化建议

### 1. 库存预扣（可选）
如果需要在下单时就锁定库存，可以：
- 下单时扣减库存
- 支付成功时不操作
- 取消或超时时恢复库存

### 2. 库存缓存（高并发场景）
- 使用 Redis 缓存库存信息
- 定期同步到数据库
- 使用分布式锁控制并发

### 3. 库存预警
- 库存低于阈值时通知商家
- 自动下架库存为0的商品

## 完成情况

✅ 支付成功时扣减库存
✅ 确认收货时增加销量
✅ 取消订单时恢复库存
✅ 超时取消时恢复库存
✅ 乐观锁防止超卖
✅ 事务保证数据一致性
✅ 完整的日志记录

所有功能已完整实现并经过测试！
