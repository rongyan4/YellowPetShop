# 📝 文件上传路径映射说明

## 🎯 配置原理

### 路径映射关系

**配置：**
```yaml
file:
  upload:
    base-dir: src/main/resources/static/images  # 物理存储目录
    url-prefix: /api/images                      # URL访问路径
```

**映射效果：**
```
URL访问路径                                    物理存储路径
/api/images/                        →         src/main/resources/static/images/
/api/images/goods/xxx.jpg           →         src/main/resources/static/images/goods/xxx.jpg
/api/images/user/avatar/xxx.jpg     →         src/main/resources/static/images/user/avatar/xxx.jpg
```

## 📂 目录结构

### 开发环境

```
petserver/
└── src/main/resources/static/images/    ← base-dir
    ├── goods/                           ← 商品图片
    │   └── goods_1_xxx.jpg
    ├── user/
    │   └── avatar/                      ← 用户头像
    │       └── user_1_xxx.jpg
    └── comment/                         ← 评论图片
        └── comment_1_xxx.jpg

访问URL: http://localhost:3000/api/images/goods/goods_1_xxx.jpg
                                 ↑
                            url-prefix
```

### 生产环境

```
/var/www/petshop/images/                 ← base-dir
├── goods/
│   └── goods_1_xxx.jpg
├── user/
│   └── avatar/
│       └── user_1_xxx.jpg
└── comment/
    └── comment_1_xxx.jpg

访问URL: https://your-domain.com/api/images/goods/goods_1_xxx.jpg
                                   ↑
                              url-prefix
```

## 🔧 配置示例

### 示例1：默认配置（开发环境）

```yaml
file:
  upload:
    base-dir: src/main/resources/static/images
    url-prefix: /api/images
```

**效果：**
- 文件保存到：`src/main/resources/static/images/goods/goods_1_xxx.jpg`
- 返回URL：`/api/images/goods/goods_1_xxx.jpg`
- 访问地址：`http://localhost:3000/api/images/goods/goods_1_xxx.jpg`

### 示例2：生产环境配置

```bash
export FILE_UPLOAD_BASE_DIR="/var/www/petshop/images"
export FILE_URL_PREFIX="/api/images"
```

**效果：**
- 文件保存到：`/var/www/petshop/images/goods/goods_1_xxx.jpg`
- 返回URL：`/api/images/goods/goods_1_xxx.jpg`
- 访问地址：`https://your-domain.com/api/images/goods/goods_1_xxx.jpg`

### 示例3：自定义路径

```bash
export FILE_UPLOAD_BASE_DIR="/data/uploads"
export FILE_URL_PREFIX="/uploads"
```

**效果：**
- 文件保存到：`/data/uploads/goods/goods_1_xxx.jpg`
- 返回URL：`/uploads/goods/goods_1_xxx.jpg`
- 访问地址：`http://localhost:3000/uploads/goods/goods_1_xxx.jpg`

## 💻 代码实现

### FileUploadConfig.java

```java
@Override
public void addResourceHandlers(ResourceHandlerRegistry registry) {
    // 判断是相对路径还是绝对路径
    String locationPath;
    if (baseDir.startsWith("/") || baseDir.contains(":")) {
        // 绝对路径
        locationPath = "file:" + baseDir + "/";
    } else {
        // 相对路径
        locationPath = "file:" + System.getProperty("user.dir") + "/" + baseDir + "/";
    }
    
    // 映射 url-prefix 到 base-dir
    registry.addResourceHandler(urlPrefix + "/**")
            .addResourceLocations(locationPath);
}
```

**说明：**
- `urlPrefix + "/**"` - 匹配所有以 url-prefix 开头的请求
- `locationPath` - 指向实际的物理存储目录
- Spring Boot 会自动将请求映射到对应的文件

## 🔍 验证配置

### 启动时查看日志

```
=== 文件上传配置 ===
URL前缀: /api/images
物理路径: file:C:/Users/rongyan/Desktop/ks/YellowPetShop/petserver/src/main/resources/static/images/
==================
```

### 测试上传

1. 上传一张图片
2. 后端返回：`/api/images/goods/goods_1_20260224120000_1234.jpg`
3. 访问：`http://localhost:3000/api/images/goods/goods_1_20260224120000_1234.jpg`
4. 应该能正常显示图片

### 检查文件

```bash
# 开发环境
ls src/main/resources/static/images/goods/

# 生产环境
ls /var/www/petshop/images/goods/
```

## ⚠️ 注意事项

### 1. 路径分隔符

- Windows：使用 `/` 或 `\` 都可以，建议使用 `/`
- Linux：必须使用 `/`

### 2. 绝对路径 vs 相对路径

**相对路径：**
```bash
FILE_UPLOAD_BASE_DIR=src/main/resources/static/images
```
- 相对于项目根目录
- 适合开发环境

**绝对路径：**
```bash
FILE_UPLOAD_BASE_DIR=/var/www/petshop/images
```
- 完整的系统路径
- 适合生产环境

### 3. 目录权限

生产环境确保目录有写权限：
```bash
sudo mkdir -p /var/www/petshop/images
sudo chown -R tomcat:tomcat /var/www/petshop/images
sudo chmod -R 755 /var/www/petshop/images
```

### 4. URL前缀

- 必须以 `/` 开头
- 不要以 `/` 结尾
- 示例：`/api/images` ✅  `/api/images/` ❌

## 🚀 完整示例

### 开发环境

**配置：**
```bash
FILE_UPLOAD_BASE_DIR=src/main/resources/static/images
FILE_URL_PREFIX=/api/images
```

**上传流程：**
1. 上传文件 `test.jpg`
2. 保存到：`src/main/resources/static/images/goods/goods_1_xxx.jpg`
3. 返回：`/api/images/goods/goods_1_xxx.jpg`
4. 前端拼接：`http://localhost:3000/api/images/goods/goods_1_xxx.jpg`
5. 浏览器访问：Spring Boot 映射到物理文件

### 生产环境

**配置：**
```bash
FILE_UPLOAD_BASE_DIR=/var/www/petshop/images
FILE_URL_PREFIX=/api/images
```

**上传流程：**
1. 上传文件 `test.jpg`
2. 保存到：`/var/www/petshop/images/goods/goods_1_xxx.jpg`
3. 返回：`/api/images/goods/goods_1_xxx.jpg`
4. 前端拼接：`https://your-domain.com/api/images/goods/goods_1_xxx.jpg`
5. Nginx 或 Spring Boot 映射到物理文件

## 🔄 与 Nginx 配合（可选）

如果使用 Nginx 做反向代理，可以让 Nginx 直接处理静态文件：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 静态图片文件（Nginx直接处理，性能更好）
    location /api/images/ {
        alias /var/www/petshop/images/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # API请求（代理到Spring Boot）
    location /api/ {
        proxy_pass http://localhost:3000/api/;
    }
    
    # 前端静态文件
    location / {
        root /var/www/petshop/dist;
        try_files $uri $uri/ /index.html;
    }
}
```

这样图片请求不会经过 Spring Boot，性能更好。

## 📊 总结

| 配置项 | 作用 | 示例 |
|--------|------|------|
| `base-dir` | 物理存储目录 | `src/main/resources/static/images` |
| `url-prefix` | URL访问路径 | `/api/images` |
| 映射关系 | `url-prefix` → `base-dir` | `/api/images/goods/xxx.jpg` → `base-dir/goods/xxx.jpg` |

配置完成后，Spring Boot 会自动将 `/api/images/**` 的请求映射到 `base-dir` 目录下的文件。
