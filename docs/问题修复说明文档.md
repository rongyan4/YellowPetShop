# 问题修复说明文档

## 修复的问题

### 1. 订单管理详情页商品价格显示NaN

**问题原因：**
订单详情页使用了错误的字段名来访问商品信息。

**修复内容：**
- 将 `item.productImage` 改为 `item.commodityPic`
- 将 `item.productName` 改为 `item.commodityName`
- 将 `item.price` 改为 `item.commodityPrice`
- 添加 `parseFloat()` 确保价格正确转换
- 添加默认值 `|| 0` 防止空值

**修复后的代码：**
```vue
<div class="goods-item" v-for="item in orderDetail.orderItems" :key="item.id">
  <img :src="item.commodityPic || '/images/goods/ml.png'" :alt="item.commodityName" class="goods-image" />
  <div class="goods-info">
    <div class="goods-name">{{ item.commodityName }}</div>
    <div class="goods-spec">数量：{{ item.quantity }}</div>
  </div>
  <div class="goods-price">¥{{ parseFloat(item.commodityPrice || 0).toFixed(2) }}</div>
  <div class="goods-quantity">x{{ item.quantity }}</div>
  <div class="goods-total">¥{{ parseFloat(item.totalPrice || 0).toFixed(2) }}</div>
</div>
```

### 2. 客户端商品详情页显示空数据

**问题原因：**
`loadGoodsDetail` 函数中使用了未定义的变量 `intPart` 和 `decimalPart`。

**修复内容：**
- 在函数开始处正确定义价格分离逻辑
- 添加空值检查和默认值
- 确保所有字段都有默认值

**修复后的代码：**
```javascript
const loadGoodsDetail = async (goodsId) => {
  try {
    const data = await getGoodDetailSafe(goodsId);
    
    if (data) {
      // 处理价格，分离整数和小数部分
      const priceStr = data.price ? data.price.toString() : '0';
      const [intPart, decimalPart = '00'] = priceStr.split('.');
      
      // 更新商品信息
      goodsInfo.value = {
        id: data.id,
        price: intPart,
        priceDecimal: decimalPart.padEnd(2, '0'),
        title: `${data.name || ''} ${data.unit || ''}`,
        name: data.name || '',
        unit: data.unit || '',
        sold: data.sold || 0,
        stock: data.stock || 0,
        msg: data.msg || '',
        shippingOrigin: data.shippingOrigin || '上海',
        postage: data.postage || 0,
        detail: data.detail || ''
      };
      
      // ... 其他代码
    }
  } catch (error) {
    console.error('加载商品详情失败:', error);
    showToast('加载商品详情失败');
  }
};
```

### 3. 商品主图上传和裁剪功能

**实现内容：**
- 添加图片上传功能
- 集成 vue-cropper 实现1:1裁剪
- 支持图片预览和重新上传
- 支持删除已上传图片

**新增功能：**

#### 图片上传区域
```vue
<div class="image-upload-area">
  <div v-if="formData.mainPicUrl" class="uploaded-image">
    <img :src="formData.mainPicUrl" alt="主图预览" />
    <div class="image-actions">
      <button type="button" @click="handleReupload" class="btn-reupload">重新上传</button>
      <button type="button" @click="formData.mainPicUrl = ''" class="btn-remove">删除</button>
    </div>
  </div>
  <div v-else class="upload-placeholder" @click="triggerFileInput">
    <svg>...</svg>
    <span>点击上传图片</span>
    <span class="upload-tip">建议尺寸：1:1，支持jpg、png格式</span>
  </div>
  <input 
    ref="fileInput" 
    type="file" 
    accept="image/*" 
    @change="handleFileSelect" 
    style="display: none;"
  />
</div>
```

#### 图片裁剪对话框
```vue
<van-popup v-model:show="showCropperDialog" position="center">
  <div class="dialog">
    <div class="dialog-header">
      <h3>裁剪图片</h3>
      <button @click="closeCropper" class="close-btn">×</button>
    </div>
    <div class="dialog-body">
      <div class="cropper-container">
        <vue-cropper
          ref="cropper"
          :img="cropperImage"
          :fixed="true"
          :fixedNumber="[1, 1]"
          :autoCrop="true"
          :autoCropWidth="300"
          :autoCropHeight="300"
        ></vue-cropper>
      </div>
    </div>
    <div class="dialog-footer">
      <button @click="closeCropper" class="btn-cancel">取消</button>
      <button @click="confirmCrop" class="btn-confirm">确定裁剪</button>
    </div>
  </div>
</van-popup>
```

#### 核心逻辑
```javascript
// 触发文件选择
const triggerFileInput = () => {
  fileInput.value?.click();
};

// 处理文件选择
const handleFileSelect = (event) => {
  const file = event.target.files[0];
  if (!file) return;
  
  // 验证文件类型
  if (!file.type.startsWith('image/')) {
    showToast('请选择图片文件');
    return;
  }
  
  // 验证文件大小（限制5MB）
  if (file.size > 5 * 1024 * 1024) {
    showToast('图片大小不能超过5MB');
    return;
  }
  
  // 读取文件并显示裁剪器
  const reader = new FileReader();
  reader.onload = (e) => {
    cropperImage.value = e.target.result;
    showCropperDialog.value = true;
  };
  reader.readAsDataURL(file);
  
  // 清空input，允许重复选择同一文件
  event.target.value = '';
};

// 确认裁剪
const confirmCrop = () => {
  cropper.value.getCropBlob((blob) => {
    // 将blob转换为base64
    const reader = new FileReader();
    reader.onload = (e) => {
      formData.value.mainPicUrl = e.target.result;
      showCropperDialog.value = false;
      showSuccessToast('图片裁剪成功');
    };
    reader.readAsDataURL(blob);
  });
};
```

## 安装依赖

需要安装 vue-cropper 组件：

```bash
cd vue-pet
npm install vue-cropper
```

或者使用 yarn：

```bash
cd vue-pet
yarn add vue-cropper
```

## 功能特点

### 图片上传
- ✅ 支持点击上传
- ✅ 文件类型验证（仅图片）
- ✅ 文件大小限制（5MB）
- ✅ 图片预览
- ✅ 重新上传
- ✅ 删除图片

### 图片裁剪
- ✅ 1:1 比例裁剪
- ✅ 可移动裁剪框
- ✅ 可缩放图片
- ✅ 高质量输出
- ✅ 实时预览
- ✅ 输出为 base64 格式

### 样式优化
- ✅ 美观的上传占位符
- ✅ 悬停效果
- ✅ 图片操作按钮
- ✅ 响应式设计

## 使用说明

### 1. 商家端商品管理

1. 进入商家端 → 商品管理
2. 点击"添加商品"或"编辑"按钮
3. 在"商品主图"区域点击上传
4. 选择图片文件
5. 在裁剪器中调整图片位置和大小
6. 点击"确定裁剪"
7. 图片将以 base64 格式保存

### 2. 订单详情查看

1. 进入商家端 → 订单管理
2. 点击订单查看详情
3. 商品信息正确显示价格和图片

### 3. 客户端商品详情

1. 客户端浏览商品
2. 点击商品进入详情页
3. 正确显示商品信息、价格、图片等

## 注意事项

1. **图片格式**：目前图片以 base64 格式存储，适合小图片。如需存储大量图片，建议：
   - 使用图片服务器（如阿里云OSS、七牛云）
   - 修改上传逻辑，将图片上传到服务器
   - 保存图片URL而非base64

2. **数据库字段**：确保数据库 `commodity` 表的 `main_pic_url` 字段类型为 `TEXT` 或 `LONGTEXT`，以支持 base64 存储。

3. **性能优化**：base64 图片会增加数据传输量，建议：
   - 限制图片大小
   - 压缩图片质量
   - 使用图片CDN

## 测试场景

### 场景1：上传新图片
1. 点击上传区域
2. 选择图片
3. 调整裁剪框
4. 确认裁剪
5. 验证图片显示正确

### 场景2：重新上传
1. 已有图片时点击"重新上传"
2. 选择新图片
3. 裁剪并确认
4. 验证新图片替换旧图片

### 场景3：删除图片
1. 点击"删除"按钮
2. 验证图片被清空
3. 显示上传占位符

### 场景4：订单详情
1. 创建包含商品的订单
2. 查看订单详情
3. 验证商品价格正确显示
4. 验证商品图片正确显示

### 场景5：商品详情
1. 访问商品详情页
2. 验证所有信息正确显示
3. 验证价格格式正确
4. 验证图片轮播正常

## 完成情况

✅ 修复订单详情页商品价格NaN问题
✅ 修复客户端商品详情页空数据问题
✅ 实现图片上传功能
✅ 实现图片1:1裁剪功能
✅ 添加图片预览和管理
✅ 优化样式和交互
✅ 添加文件验证
✅ 添加错误提示

所有问题已修复完成！
