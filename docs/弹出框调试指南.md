# 弹出框调试指南

## 问题：弹出框只变黑不显示内容

## 调试步骤：

### 1. 打开浏览器开发者工具
- 按 F12 或右键点击"检查"
- 切换到 Console（控制台）标签

### 2. 点击"宠物类型"字段
观察控制台输出，应该看到：
```
=== 点击宠物类型字段 ===
当前 showPetTypePicker 值: false
设置后 showPetTypePicker 值: true
弹出框打开
弹出框 DOM 元素: <div class="van-popup">...</div>
遮罩层 DOM 元素: <div class="van-overlay">...</div>
```

### 3. 检查关键信息

#### 3.1 检查状态值
- `showPetTypePicker` 应该从 false 变为 true
- 如果没有变化，说明响应式数据有问题

#### 3.2 检查 DOM 元素
- 弹出框和遮罩层应该都存在
- 如果为 null，说明组件没有渲染

#### 3.3 检查样式
重点关注：
- `display`: 应该是 'block' 或其他可见值，不应该是 'none'
- `zIndex`: 应该是 9999
- `opacity`: 应该是 1，不应该是 0
- `visibility`: 应该是 'visible'

### 4. 常见问题和解决方案

#### 问题1：弹出框 display 为 none
**原因**：v-model:show 绑定失败
**解决**：检查 Vant 版本，确保使用 v-model:show 而不是 v-model

#### 问题2：弹出框被遮挡（只看到黑色）
**原因**：z-index 层级问题
**解决**：
```css
/* 在全局样式或当前组件中添加 */
.van-popup {
  z-index: 9999 !important;
}
.van-overlay {
  z-index: 9998 !important;
}
```

#### 问题3：弹出框内容不显示
**原因**：van-picker 数据格式问题
**解决**：检查 columns 数据格式
```javascript
// 正确格式
const petTypeColumns = [
  { text: '猫咪', value: 'cat' },
  { text: '狗狗', value: 'dog' }
];
```

#### 问题4：点击没有反应
**原因**：事件被阻止或字段被禁用
**解决**：
- 检查是否有 @click.stop 阻止了事件冒泡
- 检查字段是否有 disabled 属性

### 5. 使用 Elements 标签检查

切换到 Elements 标签：
1. 点击"宠物类型"字段
2. 在 Elements 中搜索 "van-popup"
3. 查看元素的实际样式
4. 检查是否有其他元素覆盖在上面

### 6. 临时测试方案

在 `<script setup>` 中添加测试按钮：
```javascript
// 添加一个简单的测试
const testPopup = () => {
  alert('点击测试');
  showPetTypePicker.value = true;
};
```

在模板中添加：
```html
<button @click="testPopup" style="position: fixed; top: 100px; left: 20px; z-index: 99999;">
  测试弹出框
</button>
```

### 7. 检查 Vant 版本

在 package.json 中查看 vant 版本：
```json
"vant": "^4.9.21"
```

如果版本较低，可能需要升级：
```bash
npm install vant@latest
```

### 8. 检查是否有全局样式冲突

在浏览器控制台执行：
```javascript
// 检查所有 popup 元素
document.querySelectorAll('.van-popup').forEach(el => {
  console.log('Popup:', el);
  console.log('Display:', window.getComputedStyle(el).display);
  console.log('Z-index:', window.getComputedStyle(el).zIndex);
  console.log('Opacity:', window.getComputedStyle(el).opacity);
});
```

### 9. 最简单的测试

创建一个最简单的弹出框测试：
```vue
<template>
  <div>
    <button @click="show = true">打开</button>
    <van-popup v-model:show="show">
      <div style="padding: 50px; background: white;">
        测试内容
      </div>
    </van-popup>
  </div>
</template>

<script setup>
import { ref } from 'vue';
const show = ref(false);
</script>
```

如果这个都不显示，说明是 Vant 配置问题。

### 10. 检查路由和布局

有时候父组件的样式会影响子组件：
- 检查是否有 `overflow: hidden`
- 检查是否有 `position: fixed` 的父元素
- 检查是否有 `transform` 属性（会创建新的层叠上下文）

---

## 请按照以上步骤调试，并告诉我：
1. 控制台输出了什么？
2. DOM 元素是否存在？
3. display 和 zIndex 的值是多少？
4. 是否有报错信息？
