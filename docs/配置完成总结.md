# 🎉 文件上传配置完成总结

## ✅ 已完成的配置

### 后端配置

1. **application.yml** - 添加了文件上传配置宏
   - `file.upload.base-dir` - 文件上传根目录
   - `file.upload.domain` - 文件访问域名
   - `file.upload.url-prefix` - 文件访问路径前缀

2. **FileUploadConfig.java** - 新建配置类
   - 读取环境变量配置
   - 配置静态资源映射
   - 提供完整URL生成方法

3. **FileUploadUtil.java** - 更新工具类
   - 使用配置的根目录保存文件
   - 返回完整的图片URL（包含域名）
   - 支持不同环境的URL生成

### 前端配置

1. **vue.config.js** - 更新配置
   - 支持通过环境变量配置代理地址
   - 前端直接使用后端返回的完整URL

### 启动脚本

创建了4个启动脚本：
- `start-dev.bat` - Windows开发环境启动
- `start-dev.sh` - Linux/Mac开发环境启动
- `start-prod.bat` - Windows生产环境启动
- `start-prod.sh` - Linux/Mac生产环境启动

### 文档

1. **文件上传环境配置说明.md** - 详细配置文档
2. **环境配置快速指南.md** - 快速配置指南
3. **migrate_image_urls.sql** - 数据库迁移脚本

## 🚀 如何使用

### 开发环境（当前环境）

#### 方法1：使用启动脚本（最简单）
```batch
# Windows
start-dev.bat

# Linux/Mac
chmod +x start-dev.sh
./start-dev.sh
```

#### 方法2：手动设置环境变量
```powershell
# PowerShell
$env:FILE_DOMAIN="http://localhost:3000"
$env:DB_USERNAME="root"
$env:DB_PASSWORD="123456"
$env:JWT_SECRET="petshop_jwt_secret_key_2026"

cd petserver
mvn spring-boot:run
```

### 生产环境

1. 编辑 `start-prod.bat` 或 `start-prod.sh`
2. 修改以下配置：
   - `FILE_DOMAIN` - 改为你的实际域名（如 https://your-domain.com）
   - `FILE_UPLOAD_BASE_DIR` - 改为生产环境路径（如 /var/www/petshop/images）
   - `DB_USERNAME` 和 `DB_PASSWORD` - 改为生产数据库配置
3. 运行脚本

## 📊 配置效果

### 上传图片后的返回结果

**开发环境：**
```json
{
  "code": 200,
  "data": {
    "imageUrl": "http://localhost:3000/api/images/goods/goods_1_20260224120000_1234.jpg",
    "fileName": "goods_1_20260224120000_1234.jpg",
    "fileSize": 102400
  }
}
```

**生产环境：**
```json
{
  "code": 200,
  "data": {
    "imageUrl": "https://your-domain.com/api/images/goods/goods_1_20260224120000_1234.jpg",
    "fileName": "goods_1_20260224120000_1234.jpg",
    "fileSize": 102400
  }
}
```

### 数据库存储

数据库中存储的是完整URL：
```sql
-- 商品表
image_url: http://localhost:3000/api/images/goods/goods_1_20260224120000_1234.jpg

-- 用户表
avatar_url: http://localhost:3000/api/images/user/avatar/user_1_20260224120000_5678.jpg

-- 评论表
image_url: http://localhost:3000/api/images/comment/comment_1_20260224120000_9012.jpg
```

### 前端使用

前端直接使用后端返回的完整URL，无需拼接：
```vue
<!-- 商品图片 -->
<img :src="product.imageUrl" />

<!-- 用户头像 -->
<img :src="userInfo.avatarUrl" />

<!-- 评论图片 -->
<img :src="comment.imageUrl" />
```

## 🔄 环境切换

### 开发 → 生产

1. 修改环境变量：
   ```bash
   FILE_DOMAIN=https://your-domain.com
   FILE_UPLOAD_BASE_DIR=/var/www/petshop/images
   ```

2. 打包部署：
   ```bash
   cd petserver
   mvn clean package
   ```

3. 启动：
   ```bash
   ./start-prod.sh
   ```

### 生产 → 开发

直接运行 `start-dev.bat` 或 `start-dev.sh`

## 📝 数据迁移

如果你已有旧数据（相对路径的图片URL），需要执行迁移脚本：

1. 打开 `migrate_image_urls.sql`
2. 修改域名变量：
   ```sql
   SET @domain = 'http://localhost:3000';  -- 开发环境
   -- SET @domain = 'https://your-domain.com';  -- 生产环境
   ```
3. 执行脚本

## ⚠️ 重要提示

1. **生产环境部署前必须修改**：
   - `FILE_DOMAIN` - 改为实际域名
   - `DB_PASSWORD` - 使用强密码
   - `JWT_SECRET` - 使用随机生成的强密钥

2. **确保目录权限**：
   - 生产环境的上传目录必须存在
   - 应用需要有写权限

3. **环境变量优先级**：
   - 系统环境变量 > 启动脚本设置 > application.yml默认值

## 🎯 下一步操作

### 立即测试（开发环境）

1. 运行启动脚本：
   ```batch
   start-dev.bat
   ```

2. 访问商家端，上传一张商品图片

3. 查看返回的URL是否为完整URL（包含 http://localhost:3000）

4. 检查数据库中存储的URL是否正确

### 准备生产环境

1. 编辑 `start-prod.bat` 或 `start-prod.sh`
2. 修改域名和数据库配置
3. 在服务器上创建上传目录
4. 配置Nginx反向代理（如果需要）
5. 执行数据迁移脚本（如果有旧数据）

## 📚 相关文档

- `文件上传环境配置说明.md` - 详细配置文档
- `环境配置快速指南.md` - 快速配置指南
- `migrate_image_urls.sql` - 数据库迁移脚本

## 🎊 配置优势

✅ **统一管理** - 所有文件上传配置集中在一处  
✅ **环境隔离** - 开发和生产环境完全分离  
✅ **灵活切换** - 通过环境变量轻松切换  
✅ **完整URL** - 数据库存储完整URL，前端直接使用  
✅ **易于维护** - 修改域名只需改环境变量  
✅ **支持CDN** - 可以轻松切换到CDN域名  

配置已全部完成！现在你可以使用 `start-dev.bat` 启动开发环境进行测试了。
