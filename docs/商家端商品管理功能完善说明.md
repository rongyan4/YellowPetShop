# 商家端商品管理功能完善说明

## 功能概述

本次更新为商家端商品管理系统增加了以下完整功能：

1. **分类管理**（新增菜单和功能）
2. **商品分类字段**
3. **库存字段**
4. **商品详情管理**
5. **发货地和邮费管理**
6. **主图修改**
7. **销售单位修改**

## 数据库更新

### 1. 添加库存字段

执行 SQL 脚本：`add_stock_field.sql`

```sql
-- 为商品表添加库存字段
ALTER TABLE `commodity` ADD COLUMN `stock` int NOT NULL DEFAULT 0 COMMENT '库存数量' AFTER `sold`;

-- 为现有商品设置默认库存
UPDATE `commodity` SET `stock` = 999 WHERE `stock` = 0;
```

## 后端更新

### 1. 实体类更新

**CommodityInfo.java** - 添加库存字段：
```java
private Integer stock;
```

**CommodityDetailVO.java** - 添加库存字段：
```java
private Integer stock;
```

### 2. 新增控制器

**MerchantCategoryController.java** - 商家分类管理控制器
- `GET /api/merchant/categories` - 获取分类列表
- `POST /api/merchant/categories` - 添加分类
- `PUT /api/merchant/categories/{id}` - 更新分类
- `DELETE /api/merchant/categories/{id}` - 删除分类

### 3. 服务层更新

**CategoryService.java** - 添加方法：
- `addCategory(Category category)` - 添加分类
- `updateCategory(Category category)` - 更新分类
- `deleteCategory(Long id)` - 删除分类

**CategoryServiceImpl.java** - 实现分类管理方法

### 4. Mapper 更新

**CommodityMapper.java** - 添加方法：
```java
@Update("UPDATE commodity SET category_id = NULL WHERE category_id = #{categoryId}")
void updateCategoryToNull(@Param("categoryId") Long categoryId);
```

## 前端更新

### 1. 新增页面

**CategoryManagement.vue** - 分类管理页面
- 分类列表展示
- 添加/编辑分类
- 删除分类（会将该分类下的商品设为未分类）

### 2. 商品管理页面更新

**GoodsManagement.vue** - 功能增强：

#### 表格列更新
- 添加"库存"列，库存低于10时红色显示
- 保留"分类"列

#### 表单字段更新
- **商品名称**（必填）
- **商品分类**（下拉选择）
- **销售单位**（如：件、盒、袋）
- **商品价格**（必填）
- **库存数量**（必填）
- **发货地**（默认：上海）
- **邮费**（0表示包邮）
- **主图URL**（带预览）
- **商品描述**（简短描述）
- **商品详情**（支持HTML富文本）

### 3. 路由更新

**router/index.js** - 添加分类管理路由：
```javascript
{
  path: "category",
  name: "merchant-category",
  component: () => import("../views/merchant/CategoryManagement.vue"),
  meta: { requiresMerchantAuth: true }
}
```

### 4. 侧边栏菜单更新

**MerchantLayout.vue** - 添加分类管理菜单项：
```javascript
{
  name: '分类管理',
  path: '/merchant/category',
  icon: 'M4 4h7v7H4V4z M13 4h7v7h-7V4z M4 13h7v7H4v-7z M13 13h7v7h-7v-7z'
}
```

### 5. API 更新

**merchant.js** - 添加分类管理接口：
- `getCategoryList()` - 获取分类列表
- `addCategory(data)` - 添加分类
- `updateCategory(categoryId, data)` - 更新分类
- `deleteCategory(categoryId)` - 删除分类

### 6. 客户端商品详情更新

**GoodDetailsView.vue** - 显示库存信息（内部使用，不直接展示给用户）

## 使用说明

### 1. 数据库初始化

```bash
# 在 MySQL 中执行
mysql -u root -p petshop < add_stock_field.sql
```

### 2. 启动后端服务

```bash
cd petserver
mvn spring-boot:run
```

### 3. 启动前端服务

```bash
cd vue-pet
npm install
npm run serve
```

### 4. 访问商家端

1. 访问：`http://localhost:8080/merchant/login`
2. 登录后进入商家管理系统
3. 点击"分类管理"菜单管理商品分类
4. 点击"商品管理"菜单管理商品

## 功能特点

### 分类管理
- ✅ 添加、编辑、删除分类
- ✅ 设置分类图标和排序
- ✅ 删除分类时自动处理关联商品

### 商品管理
- ✅ 完整的商品信息管理
- ✅ 支持商品分类
- ✅ 库存管理（低库存提醒）
- ✅ 发货地和邮费设置
- ✅ 主图上传和预览
- ✅ 富文本商品详情
- ✅ 销售单位自定义
- ✅ 商品上下架管理

### 客户端展示
- ✅ 商品详情页显示发货地和邮费
- ✅ 商品详情页显示销售单位
- ✅ 商品详情页显示主图
- ✅ 商品详情页显示富文本详情

## 注意事项

1. **库存管理**：添加商品时必须填写库存，库存低于10会红色提醒
2. **分类删除**：删除分类时，该分类下的商品会变为"未分类"状态
3. **主图URL**：需要输入完整的图片URL地址
4. **商品详情**：支持HTML格式，可以插入图片、文字等
5. **邮费设置**：输入0表示包邮，大于0表示需要支付邮费

## API 接口列表

### 分类管理
- `GET /api/merchant/categories` - 获取分类列表
- `POST /api/merchant/categories` - 添加分类
- `PUT /api/merchant/categories/{id}` - 更新分类
- `DELETE /api/merchant/categories/{id}` - 删除分类

### 商品管理（已有接口保持不变）
- `GET /api/merchant/goods/list` - 获取商品列表
- `POST /api/merchant/goods/add` - 添加商品
- `PUT /api/merchant/goods/update` - 更新商品
- `DELETE /api/merchant/goods/delete/{id}` - 删除商品

## 完成情况

✅ 数据库添加库存字段
✅ 后端实体类更新
✅ 分类管理控制器
✅ 分类管理服务
✅ 商品管理表单完善
✅ 分类管理页面
✅ 路由配置
✅ 菜单更新
✅ API 接口
✅ 客户端商品详情展示

所有功能已完整实现并可以正常使用！
