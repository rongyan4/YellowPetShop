# 数据库修复说明

## 问题描述

系统出现以下数据库字段缺失错误：

1. **comment 表缺失字段**：
   - `merchant_reply` - 商家回复内容
   - `merchant_reply_time` - 商家回复时间

2. **orders 表缺失字段**：
   - `original_amount` - 原始金额（改价前）
   - `price_modified` - 是否已改价标志

## 修复步骤

### 方法一：执行完整修复脚本（推荐）

执行项目根目录下的 `database_complete_fix.sql` 脚本，该脚本会自动检查并添加所有缺失的字段：

```bash
# 在MySQL命令行中执行
mysql -u root -p your_database_name < database_complete_fix.sql
```

或者在MySQL客户端工具（如Navicat、MySQL Workbench）中：
1. 打开 `database_complete_fix.sql` 文件
2. 选择对应的数据库
3. 执行整个脚本

### 方法二：手动添加字段

如果只想添加缺失的字段，可以执行以下SQL语句：

```sql
-- 为 comment 表添加商家回复字段
ALTER TABLE `comment` 
ADD COLUMN `merchant_reply` varchar(500) NULL DEFAULT NULL COMMENT '商家回复' AFTER `content`,
ADD COLUMN `merchant_reply_time` datetime NULL DEFAULT NULL COMMENT '商家回复时间' AFTER `merchant_reply`;

-- 为 orders 表添加改价相关字段
ALTER TABLE `orders` 
ADD COLUMN `original_amount` decimal(10,2) NULL DEFAULT NULL COMMENT '原始金额（改价前）' AFTER `total_amount`,
ADD COLUMN `price_modified` tinyint(1) NOT NULL DEFAULT 0 COMMENT '是否已改价 1-是 0-否' AFTER `original_amount`;
```

## 验证修复

执行以下SQL语句验证字段是否添加成功：

```sql
-- 检查 comment 表结构
DESC comment;

-- 检查 orders 表结构
DESC orders;
```

## 其他修复

### 1. 全局异常处理器

已创建 `GlobalExceptionHandler.java` 文件，用于统一处理系统异常，确保：
- SQL异常返回500状态码而不是200
- 数据访问异常被正确捕获和记录
- 所有未处理的异常都会返回适当的错误信息

位置：`petserver/src/main/java/com/yellow/petshop/exception/GlobalExceptionHandler.java`

### 2. 重启应用

修复完成后，请重启Spring Boot应用以使更改生效。

## 注意事项

- 执行SQL脚本前请备份数据库
- 确保数据库连接配置正确
- 如果字段已存在，脚本会自动跳过，不会报错
