# 钱包系统完整实现文档

## 功能概述

钱包系统提供了完整的用户钱包管理功能，包括余额管理、支付密码、交易记录、充值提现等。

## 数据库设计

### 1. user_wallet 表（用户钱包表）

```sql
CREATE TABLE `user_wallet` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '钱包ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `balance` DECIMAL(10, 2) NOT NULL DEFAULT 0.00 COMMENT '余额',
  `pay_password` VARCHAR(255) DEFAULT NULL COMMENT '支付密码（加密）',
  `is_locked` TINYINT(1) DEFAULT 0 COMMENT '是否锁定：0-否，1-是',
  `lock_time` DATETIME DEFAULT NULL COMMENT '锁定时间',
  `error_count` INT DEFAULT 0 COMMENT '密码错误次数',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户钱包表';
```

### 2. wallet_transaction 表（钱包交易记录表）

```sql
CREATE TABLE `wallet_transaction` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '交易ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `type` VARCHAR(20) NOT NULL COMMENT '交易类型',
  `amount` DECIMAL(10, 2) NOT NULL COMMENT '交易金额',
  `balance_before` DECIMAL(10, 2) NOT NULL COMMENT '交易前余额',
  `balance_after` DECIMAL(10, 2) NOT NULL COMMENT '交易后余额',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='钱包交易记录表';
```

**交易类型说明：**
- `RECHARGE` - 充值
- `WITHDRAW` - 提现
- `DEDUCT` - 扣款（订单支付等）
- `ADD` - 增加（退款等）

## 后端API接口

### 1. 获取钱包信息

**接口地址：** `GET /api/wallet/{userId}`

**请求参数：**
- `userId` - 用户ID（路径参数）

**返回示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "balance": 1000.00,
    "isLocked": false,
    "errorCount": 0,
    "createTime": "2024-01-01 00:00:00"
  }
}
```

### 2. 获取当前用户钱包信息

**接口地址：** `GET /api/wallet/info`

**请求头：**
- `Authorization: Bearer {token}`

**返回示例：** 同上

### 3. 设置支付密码

**接口地址：** `POST /api/wallet/set-pay-password`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "password": "123456"
}
```

**返回示例：**
```json
{
  "code": 200,
  "msg": "设置成功",
  "data": null
}
```

### 4. 修改支付密码

**接口地址：** `POST /api/wallet/update-pay-password`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

**返回示例：**
```json
{
  "code": 200,
  "msg": "修改成功",
  "data": null
}
```

### 5. 验证支付密码

**接口地址：** `POST /api/wallet/verify-pay-password`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "password": "123456"
}
```

**返回示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": true
}
```

**注意事项：**
- 密码错误5次后账户将被锁定30分钟
- 锁定期间无法进行任何需要验证密码的操作

### 6. 获取交易记录

**接口地址：** `GET /api/wallet/transactions`

**请求头：**
- `Authorization: Bearer {token}`

**请求参数：**
- `page` - 页码（默认1）
- `pageSize` - 每页数量（默认10）

**返回示例：**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "userId": 1,
        "type": "RECHARGE",
        "amount": 100.00,
        "balanceBefore": 0.00,
        "balanceAfter": 100.00,
        "remark": "充值",
        "createTime": "2024-01-01 00:00:00"
      }
    ],
    "total": 1,
    "page": 1,
    "pageSize": 10
  }
}
```

### 7. 充值

**接口地址：** `POST /api/wallet/recharge`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "amount": 100.00
}
```

**返回示例：**
```json
{
  "code": 200,
  "msg": "充值成功",
  "data": null
}
```

### 8. 提现

**接口地址：** `POST /api/wallet/withdraw`

**请求头：**
- `Authorization: Bearer {token}`

**请求体：**
```json
{
  "amount": 50.00,
  "password": "123456"
}
```

**返回示例：**
```json
{
  "code": 200,
  "msg": "提现申请已提交",
  "data": null
}
```

## 前端API调用

### 引入API
```javascript
import { 
  getWalletInfo, 
  getCurrentWalletInfo,
  setPayPassword,
  updatePayPassword,
  verifyPayPassword,
  getWalletTransactions,
  recharge,
  withdraw
} from '@/api/wallet';
```

### 使用示例

#### 1. 获取钱包信息
```javascript
const loadWalletInfo = async () => {
  try {
    const response = await getCurrentWalletInfo();
    if (response.code === 200) {
      walletInfo.value = response.data;
    }
  } catch (error) {
    console.error('获取钱包信息失败', error);
  }
};
```

#### 2. 设置支付密码
```javascript
const handleSetPassword = async (password) => {
  try {
    const response = await setPayPassword({ password });
    if (response.code === 200) {
      showSuccessToast('设置成功');
    }
  } catch (error) {
    showFailToast('设置失败');
  }
};
```

#### 3. 充值
```javascript
const handleRecharge = async (amount) => {
  try {
    const response = await recharge({ amount });
    if (response.code === 200) {
      showSuccessToast('充值成功');
      loadWalletInfo(); // 刷新钱包信息
    }
  } catch (error) {
    showFailToast('充值失败');
  }
};
```

#### 4. 提现
```javascript
const handleWithdraw = async (amount, password) => {
  try {
    const response = await withdraw({ amount, password });
    if (response.code === 200) {
      showSuccessToast('提现申请已提交');
      loadWalletInfo(); // 刷新钱包信息
    }
  } catch (error) {
    showFailToast('提现失败');
  }
};
```

## 商家端钱包管理

商家可以通过会员管理详情页对用户钱包进行管理：

### 1. 调整会员余额

**接口地址：** `POST /api/merchant/members/{userId}/balance`

**请求体：**
```json
{
  "amount": 100.00,  // 正数增加，负数减少
  "remark": "活动奖励"
}
```

### 2. 重置会员支付密码

**接口地址：** `POST /api/merchant/members/{userId}/reset-pay-password`

**请求体：**
```json
{
  "newPassword": "123456"
}
```

## 安全机制

### 1. 支付密码加密
- 使用BCrypt算法加密存储
- 密码不可逆，只能重置

### 2. 防暴力破解
- 密码错误5次后锁定账户
- 锁定时间30分钟
- 锁定期间无法进行任何操作

### 3. 交易记录
- 所有余额变动都会记录
- 记录交易前后余额
- 便于对账和审计

### 4. 事务保证
- 所有涉及余额变动的操作都使用事务
- 保证数据一致性

## 业务流程

### 1. 用户充值流程
1. 用户选择充值金额
2. 调用充值接口
3. 系统增加用户余额
4. 记录交易记录
5. 返回充值成功

### 2. 用户提现流程
1. 用户输入提现金额和支付密码
2. 验证支付密码
3. 检查余额是否充足
4. 扣除用户余额
5. 记录交易记录
6. 提交提现申请

### 3. 订单支付流程
1. 用户选择钱包支付
2. 验证支付密码
3. 检查余额是否充足
4. 扣除订单金额
5. 记录交易记录
6. 更新订单状态

### 4. 订单退款流程
1. 订单取消或退款
2. 增加用户余额
3. 记录交易记录
4. 通知用户

## 注意事项

1. **金额精度**：使用 `DECIMAL(10, 2)` 存储，保留两位小数
2. **并发控制**：使用数据库事务保证并发安全
3. **密码安全**：支付密码必须是6位数字，使用BCrypt加密
4. **余额检查**：扣款前必须检查余额是否充足
5. **交易记录**：所有余额变动必须记录交易记录
6. **错误处理**：所有异常都要捕获并返回友好的错误信息

## 扩展功能

### 1. 钱包冻结
可以添加冻结金额字段，用于订单待支付等场景

### 2. 积分系统
可以扩展积分字段，实现积分兑换等功能

### 3. 优惠券
可以关联优惠券系统，实现优惠券抵扣

### 4. 分账系统
可以实现商家分账、平台抽成等功能

## 测试建议

1. **单元测试**：测试各个服务方法
2. **集成测试**：测试完整的业务流程
3. **压力测试**：测试高并发场景
4. **安全测试**：测试密码加密、防暴力破解等

## 常见问题

### Q1: 如何处理余额不足？
A: 在扣款前检查余额，如果不足则抛出异常，事务回滚。

### Q2: 如何防止重复扣款？
A: 使用数据库事务和唯一订单号，确保幂等性。

### Q3: 支付密码忘记了怎么办？
A: 只能通过商家后台重置，不支持找回。

### Q4: 如何处理并发充值？
A: 使用数据库事务和乐观锁，保证数据一致性。
