# 商品图片上传功能实现说明

## 功能概述

实现了完整的商品图片上传功能，图片保存到后端服务器的 `static/images/goods` 目录。

## 实现内容

### 1. 后端实现

#### 图片上传接口
**路径：** `POST /api/merchant/goods/upload-image`

**功能：**
- 接收 multipart/form-data 格式的图片文件
- 验证文件类型（仅图片）
- 验证文件大小（最大5MB）
- 生成唯一文件名（UUID）
- 保存到 `static/images/goods` 目录
- 返回图片访问URL

**代码位置：** `MerchantGoodsController.java`

```java
@PostMapping("/upload-image")
public Result<Map<String, String>> uploadImage(@RequestParam("file") MultipartFile file) {
    // 验证文件
    // 生成唯一文件名
    // 保存文件
    // 返回URL
}
```

#### 配置项
在 `application.yml` 中可配置上传路径：
```yaml
file:
  upload:
    path: src/main/resources/static/images/goods
```

### 2. 前端实现

#### 图片上传流程
1. **选择图片** → 用户点击上传区域
2. **文件验证** → 验证类型和大小
3. **显示裁剪器** → 使用 cropperjs 裁剪为1:1
4. **裁剪图片** → 用户调整裁剪区域
5. **上传服务器** → 将裁剪后的图片上传
6. **保存URL** → 保存服务器返回的图片URL

#### API 调用
**文件：** `src/api/merchant.js`

```javascript
export function uploadGoodsImage(file) {
  const formData = new FormData();
  formData.append('file', file);
  
  return request({
    url: '/merchant/goods/upload-image',
    method: 'post',
    data: formData,
    headers: {
      'Content-Type': 'multipart/form-data'
    }
  });
}
```

#### 裁剪并上传
**文件：** `GoodsManagement.vue`

```javascript
const confirmCrop = async () => {
  // 1. 获取裁剪后的canvas
  const canvas = cropperInstance.getCroppedCanvas({
    width: 800,
    height: 800,
    imageSmoothingQuality: 'high',
  });
  
  // 2. 转换为blob
  canvas.toBlob(async (blob) => {
    // 3. 创建File对象
    const file = new File([blob], 'goods-image.jpg', { type: 'image/jpeg' });
    
    // 4. 上传到服务器
    const result = await uploadGoodsImage(file);
    
    // 5. 保存返回的URL
    formData.value.mainPicUrl = result.data.url;
  }, 'image/jpeg', 0.9);
};
```

### 3. 图片访问

#### 后端静态资源配置
Spring Boot 自动配置了 `static` 目录为静态资源目录。

图片访问路径：
```
http://localhost:8081/images/goods/xxx.jpg
```

#### 前端图片显示
```javascript
const getImageUrl = (url) => {
  if (!url) return '';
  // 如果是完整URL，直接返回
  if (url.startsWith('http://') || url.startsWith('https://')) {
    return url;
  }
  // 如果是相对路径，拼接服务器地址
  return `http://localhost:8081${url}`;
};
```

## 数据流程

### 添加商品流程
```
1. 用户上传图片
   ↓
2. 裁剪为1:1
   ↓
3. 上传到服务器
   ↓
4. 获取图片URL: /images/goods/xxx.jpg
   ↓
5. 填写其他商品信息
   ↓
6. 提交商品数据（包含图片URL）
   ↓
7. 保存到数据库
```

### 编辑商品流程
```
1. 加载商品信息（包含图片URL）
   ↓
2. 显示现有图片
   ↓
3. 如需更换：重新上传 → 裁剪 → 上传 → 获取新URL
   ↓
4. 提交更新（包含新图片URL）
   ↓
5. 更新数据库
```

### 显示商品流程
```
1. 从数据库读取商品信息
   ↓
2. 获取图片URL: /images/goods/xxx.jpg
   ↓
3. 拼接完整URL: http://localhost:8081/images/goods/xxx.jpg
   ↓
4. 在页面显示图片
```

## 文件结构

```
petserver/
└── src/main/resources/static/images/goods/
    ├── uuid1.jpg  (商品图片1)
    ├── uuid2.jpg  (商品图片2)
    └── ...
```

## 特性

### 后端特性
- ✅ 文件类型验证
- ✅ 文件大小限制（5MB）
- ✅ 唯一文件名（UUID）
- ✅ 自动创建目录
- ✅ 错误处理
- ✅ 返回访问URL

### 前端特性
- ✅ 图片裁剪（1:1比例）
- ✅ 高质量输出（800x800）
- ✅ 上传进度提示
- ✅ 图片预览
- ✅ 重新上传
- ✅ 删除图片
- ✅ 自动拼接URL

## 配置说明

### 修改上传路径
在 `application.yml` 中：
```yaml
file:
  upload:
    path: /your/custom/path
```

### 修改文件大小限制
在 `application.yml` 中：
```yaml
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
```

### 修改图片质量
在 `GoodsManagement.vue` 中：
```javascript
canvas.toBlob(async (blob) => {
  // ...
}, 'image/jpeg', 0.9); // 0.9 = 90%质量，可调整为0.1-1.0
```

### 修改输出尺寸
在 `GoodsManagement.vue` 中：
```javascript
const canvas = cropperInstance.getCroppedCanvas({
  width: 800,  // 修改宽度
  height: 800, // 修改高度
  imageSmoothingQuality: 'high',
});
```

## 注意事项

1. **目录权限**：确保应用有权限写入 `static/images/goods` 目录
2. **磁盘空间**：注意监控磁盘空间使用情况
3. **文件清理**：建议定期清理未使用的图片文件
4. **URL配置**：生产环境需要修改 `getImageUrl` 中的服务器地址
5. **CORS配置**：如果前后端分离部署，需要配置CORS

## 优化建议

### 1. 使用CDN
将图片上传到CDN服务（如阿里云OSS、七牛云）：
- 减轻服务器压力
- 加快图片加载速度
- 节省带宽成本

### 2. 图片压缩
在上传前进行压缩：
- 减少存储空间
- 加快上传速度
- 减少带宽消耗

### 3. 缩略图
生成多种尺寸的缩略图：
- 列表页使用小图
- 详情页使用大图
- 提升加载速度

### 4. 图片懒加载
在前端实现图片懒加载：
- 提升页面加载速度
- 减少初始流量消耗

## 测试场景

### 场景1：上传新图片
1. 点击上传区域
2. 选择图片文件
3. 调整裁剪区域
4. 确认裁剪
5. 验证图片上传成功
6. 验证图片显示正确

### 场景2：更换图片
1. 编辑已有商品
2. 点击"重新上传"
3. 选择新图片
4. 裁剪并上传
5. 验证新图片替换成功

### 场景3：删除图片
1. 点击"删除"按钮
2. 验证图片被清空
3. 显示上传占位符

### 场景4：文件验证
1. 尝试上传非图片文件 → 提示错误
2. 尝试上传超大文件 → 提示错误
3. 上传正常图片 → 成功

## 完成情况

✅ 后端图片上传接口
✅ 文件验证和保存
✅ 前端图片裁剪
✅ 图片上传到服务器
✅ 图片URL管理
✅ 图片预览和显示
✅ 完整的错误处理
✅ 用户友好的提示

所有功能已完整实现！
