# 前端功能完善总结

## 已完成的更新

### 1. 商家端数据概览页面 (Dashboard.vue)

**更新内容**：
- ✅ 集成后端 `/merchant/dashboard` 接口
- ✅ 显示实时统计数据：
  - 商品总数（上架/下架）
  - 今日订单数和销售额
  - 订单总数和总销售额
  - 评论总数和待回复数
- ✅ 添加待处理事项快捷入口
- ✅ 点击可跳转到对应订单列表

**使用的API**：
```javascript
import { getDashboard } from '@/api/merchant';
```

### 2. 商品详情页面 (GoodDetailsView.vue)

**已有功能**：
- ✅ 显示商品销量 (`goodsInfo.sold`)
- ✅ 显示评论列表
- ✅ 支持评论图片展示
- ✅ 评论分页加载

**需要添加的功能**：
- 📝 客户端评论功能（创建评论）

### 3. 订单管理页面 (OrderManagement.vue)

**需要更新**：
- 📝 适配新的API路径
- 📝 订单改价功能
- 📝 订单发货功能
- 📝 订单详情查看

### 4. 商品管理页面 (GoodsManagement.vue)

**需要添加**：
- 📝 商品上下架按钮
- 📝 商品评论查看入口
- 📝 评论管理功能

## 需要创建的新页面

### 1. 商品评论管理页面
**路径**: `/merchant/goods/:id/comments`

**功能**：
- 查看商品所有评论
- 回复评论
- 删除评论
- 置顶评论

### 2. 订单详情页面（商家端）
**路径**: `/merchant/orders/:id`

**功能**：
- 查看完整订单信息
- 订单改价（待支付）
- 订单发货（已付款）
- 查看物流信息

### 3. 客户端评论组件
**位置**: 商品详情页底部

**功能**：
- 评分（1-5星）
- 评论内容输入
- 上传评论图片
- 提交评论

## API接口使用说明

### 商家端API (merchant.js)

```javascript
// 数据概览
import { getDashboard } from '@/api/merchant';
const dashboard = await getDashboard();

// 订单列表
import { getOrderList } from '@/api/merchant';
const orders = await getOrderList({ status: 'pending', page: 1, size: 10 });

// 订单详情
import { getOrderDetail } from '@/api/merchant';
const order = await getOrderDetail(orderId);

// 订单改价
import { updateOrderPrice } from '@/api/merchant';
await updateOrderPrice({
  orderId: 1,
  newTotalAmount: 100.00,
  newPostage: 10.00,
  reason: '优惠活动'
});

// 订单发货
import { shipOrder } from '@/api/merchant';
await shipOrder({
  orderId: 1,
  shippingCompany: '顺丰速运',
  trackingNo: 'SF1234567890',
  remark: '已发货'
});

// 商品上下架
import { updateProductStatus } from '@/api/merchant';
await updateProductStatus(productId, true); // true=上架, false=下架

// 获取商品评论
import { getProductComments } from '@/api/merchant';
const comments = await getProductComments(productId, { page: 1, size: 10 });

// 回复评论
import { replyComment } from '@/api/merchant';
await replyComment({
  commentId: 1,
  reply: '感谢您的评价'
});

// 删除评论
import { deleteComment } from '@/api/merchant';
await deleteComment(commentId);

// 置顶评论
import { topComment } from '@/api/merchant';
await topComment(commentId, true); // true=置顶, false=取消置顶
```

### 客户端API (comment.js)

```javascript
// 创建评论
import { createComment } from '@/api/comment';
await createComment({
  commodityId: 1,
  orderId: 100,
  star: 5,
  content: '商品很好',
  images: ['url1', 'url2']
});

// 查看评论
import { getCommentsByPage } from '@/api/comment';
const comments = await getCommentsByPage(commodityId, 1, 10);
```

## 下一步工作

### 优先级1：核心功能
1. ✅ 更新商家端数据概览页面
2. 📝 更新订单管理页面（改价、发货）
3. 📝 添加客户端评论功能
4. 📝 添加商品上下架按钮

### 优先级2：增强功能
5. 📝 创建订单详情页面（商家端）
6. 📝 创建商品评论管理页面
7. 📝 优化商品详情页显示销量

### 优先级3：体验优化
8. 📝 添加加载状态提示
9. 📝 添加错误处理
10. 📝 添加操作成功提示

## 注意事项

1. **Token验证**：所有商家端接口需要携带商家Token
2. **权限控制**：客户端评论需要用户登录
3. **数据验证**：
   - 改价只能对待支付订单
   - 发货只能对已付款订单
   - 评论只能对已完成订单
4. **错误处理**：使用 `showToast` 显示错误信息
5. **成功提示**：使用 `showSuccessToast` 显示成功信息

## 测试建议

1. 测试数据概览页面数据加载
2. 测试订单改价功能（待支付订单）
3. 测试订单发货功能（已付款订单）
4. 测试商品上下架功能
5. 测试客户端评论功能
6. 测试商家端评论管理功能

## 已更新的文件

- ✅ `vue-pet/src/views/merchant/Dashboard.vue` - 商家端数据概览
- ✅ `vue-pet/src/api/merchant.js` - 商家端API接口
- ✅ `vue-pet/src/api/merchantOrder.js` - 订单API接口（路径更新）
- ✅ `vue-pet/src/api/comment.js` - 评论API接口

## 待更新的文件

- 📝 `vue-pet/src/views/merchant/OrderManagement.vue` - 订单管理页面
- 📝 `vue-pet/src/views/merchant/GoodsManagement.vue` - 商品管理页面
- 📝 `vue-pet/src/views/GoodDetailsView.vue` - 商品详情页（添加评论功能）
- 📝 创建新页面：订单详情、评论管理等
