# 🎉 文件上传配置完成总结（前后端分离方案）

## ✅ 配置方案

### 架构设计

**数据存储：** 数据库存储相对路径（如：`/api/images/goods/goods_1_xxx.jpg`）  
**后端返回：** 返回相对路径给前端  
**前端处理：** 通过环境变量配置域名，拼接完整URL  

### 优势

✅ **灵活切换** - 可以轻松切换域名、CDN  
✅ **环境隔离** - 开发、测试、生产环境完全分离  
✅ **数据独立** - 数据库数据不依赖域名  
✅ **易于迁移** - 更换域名无需修改数据库  
✅ **支持CDN** - 前端可配置CDN域名  

## 📦 已完成的配置

### 后端配置

1. **application.yml** - 文件上传配置
```yaml
file:
  upload:
    base-dir: ${FILE_UPLOAD_BASE_DIR:src/main/resources/static/images}
    url-prefix: ${FILE_URL_PREFIX:/api/images}
```

2. **FileUploadConfig.java** - 配置类
   - 读取环境变量配置
   - 配置静态资源映射
   - 提供相对路径URL生成方法

3. **FileUploadUtil.java** - 工具类
   - 使用配置的根目录保存文件
   - 返回相对路径URL（不包含域名）

### 前端配置

1. **request.js** - 添加图片基础URL配置
```javascript
export const IMAGE_BASE_URL = process.env.VUE_APP_IMAGE_BASE_URL || 'http://localhost:3000';
export const getImageUrl = (relativePath) => IMAGE_BASE_URL + relativePath;
```

2. **image.js** - 图片工具类（新建）
   - `getImageUrl()` - 获取完整图片URL
   - `getImageUrls()` - 批量获取图片URL
   - `getImageBaseUrl()` - 获取图片基础URL

3. **vue.config.js** - 支持环境变量配置代理

### 启动脚本

- `start-dev.bat` / `start-dev.sh` - 开发环境启动
- `start-prod.bat` / `start-prod.sh` - 生产环境启动

### 文档

- `migrate_image_urls.sql` - 数据库迁移脚本

## 🚀 如何使用

### 开发环境

#### 方法1：使用启动脚本（推荐）
```batch
# Windows
start-dev.bat

# Linux/Mac
chmod +x start-dev.sh
./start-dev.sh
```

#### 方法2：手动设置环境变量

**后端（PowerShell）：**
```powershell
$env:FILE_UPLOAD_BASE_DIR="src/main/resources/static/images"
$env:FILE_URL_PREFIX="/api/images"
$env:DB_USERNAME="root"
$env:DB_PASSWORD="123456"
$env:JWT_SECRET="petshop_jwt_secret_key_2026"

cd petserver
mvn spring-boot:run
```

**前端（新开终端）：**
```powershell
$env:VUE_APP_IMAGE_BASE_URL="http://localhost:3000"

cd vue-pet
npm run serve
```

### 生产环境

#### 后端配置

编辑 `start-prod.bat` 或 `start-prod.sh`：
```bash
export FILE_UPLOAD_BASE_DIR="/var/www/petshop/images"
export FILE_URL_PREFIX="/api/images"
export DB_USERNAME="your_db_user"
export DB_PASSWORD="your_db_password"
export JWT_SECRET="your_jwt_secret"
```

#### 前端配置

创建 `.env.production` 文件（vue-pet目录）：
```properties
NODE_ENV=production
VUE_APP_IMAGE_BASE_URL=https://your-domain.com
```

或者在打包时指定：
```bash
VUE_APP_IMAGE_BASE_URL=https://your-domain.com npm run build
```

## 📊 数据流程

### 上传流程

1. **前端上传图片** → 后端
2. **后端保存文件** → `src/main/resources/static/images/goods/goods_1_xxx.jpg`
3. **后端返回相对路径** → `/api/images/goods/goods_1_xxx.jpg`
4. **数据库存储相对路径** → `/api/images/goods/goods_1_xxx.jpg`

### 显示流程

1. **后端返回相对路径** → `/api/images/goods/goods_1_xxx.jpg`
2. **前端拼接域名** → `http://localhost:3000/api/images/goods/goods_1_xxx.jpg`
3. **浏览器显示图片**

## 💻 前端使用示例

### 方法1：使用图片工具类（推荐）

```vue
<template>
  <div>
    <!-- 商品图片 -->
    <img :src="getImageUrl(product.imageUrl)" />
    
    <!-- 用户头像 -->
    <img :src="getImageUrl(userInfo.avatarUrl)" />
    
    <!-- 评论图片 -->
    <img :src="getImageUrl(comment.imageUrl)" />
  </div>
</template>

<script>
import { getImageUrl } from '@/utils/image';

export default {
  setup() {
    return {
      getImageUrl
    };
  }
};
</script>
```

### 方法2：使用计算属性

```vue
<template>
  <img :src="fullImageUrl" />
</template>

<script>
import { getImageUrl } from '@/utils/image';

export default {
  props: {
    imageUrl: String
  },
  computed: {
    fullImageUrl() {
      return getImageUrl(this.imageUrl);
    }
  }
};
</script>
```

### 方法3：直接在request.js中导入

```javascript
import { getImageUrl } from '@/utils/request';

// 使用
const fullUrl = getImageUrl('/api/images/goods/goods_1_xxx.jpg');
```

## 🔄 环境变量配置

### 后端环境变量

| 变量名 | 开发环境 | 生产环境 | 说明 |
|--------|---------|---------|------|
| `FILE_UPLOAD_BASE_DIR` | `src/main/resources/static/images` | `/var/www/petshop/images` | 文件上传根目录 |
| `FILE_URL_PREFIX` | `/api/images` | `/api/images` | URL路径前缀 |
| `DB_USERNAME` | `root` | `your_db_user` | 数据库用户名 |
| `DB_PASSWORD` | `123456` | `your_db_password` | 数据库密码 |
| `JWT_SECRET` | `petshop_jwt_secret_key_2026` | `your_jwt_secret` | JWT密钥 |

### 前端环境变量

| 变量名 | 开发环境 | 生产环境 | 说明 |
|--------|---------|---------|------|
| `VUE_APP_IMAGE_BASE_URL` | `http://localhost:3000` | `https://your-domain.com` | 图片基础URL |

## 📝 数据示例

### 后端返回数据

```json
{
  "code": 200,
  "data": {
    "imageUrl": "/api/images/goods/goods_1_20260224120000_1234.jpg",
    "fileName": "goods_1_20260224120000_1234.jpg",
    "fileSize": 102400
  }
}
```

### 数据库存储

```sql
-- 商品表
image_url: /api/images/goods/goods_1_20260224120000_1234.jpg

-- 用户表
avatar_url: /api/images/user/avatar/user_1_20260224120000_5678.jpg

-- 评论表
image_url: /api/images/comment/comment_1_20260224120000_9012.jpg
```

### 前端显示

```javascript
// 开发环境
getImageUrl('/api/images/goods/goods_1_xxx.jpg')
// 返回: http://localhost:3000/api/images/goods/goods_1_xxx.jpg

// 生产环境
getImageUrl('/api/images/goods/goods_1_xxx.jpg')
// 返回: https://your-domain.com/api/images/goods/goods_1_xxx.jpg
```

## 🔄 环境切换

### 切换到CDN

只需修改前端环境变量：
```bash
VUE_APP_IMAGE_BASE_URL=https://cdn.your-domain.com
```

数据库和后端无需任何修改！

### 切换域名

只需修改前端环境变量：
```bash
VUE_APP_IMAGE_BASE_URL=https://new-domain.com
```

数据库和后端无需任何修改！

## 📝 数据迁移

如果你已有旧数据（完整URL），执行迁移脚本：

```sql
-- 打开 migrate_image_urls.sql 并执行
-- 会将完整URL转换为相对路径
-- 例如：http://localhost:3000/api/images/goods/xxx.jpg -> /api/images/goods/xxx.jpg
```

## ⚠️ 重要提示

1. **生产环境部署前必须配置**：
   - 后端：设置 `FILE_UPLOAD_BASE_DIR` 为生产路径
   - 前端：设置 `VUE_APP_IMAGE_BASE_URL` 为生产域名

2. **前端打包时**：
```bash
# 开发环境打包
npm run build

# 生产环境打包（指定域名）
VUE_APP_IMAGE_BASE_URL=https://your-domain.com npm run build
```

3. **Nginx配置示例**（生产环境）：
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端静态文件
    location / {
        root /var/www/petshop/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端API代理
    location /api/ {
        proxy_pass http://localhost:3000/api/;
    }
}
```

## 🎯 下一步操作

### 立即测试（开发环境）

1. 运行启动脚本：
```batch
start-dev.bat
```

2. 访问商家端，上传一张商品图片

3. 查看返回的URL：应该是 `/api/images/goods/goods_1_xxx.jpg`

4. 检查前端显示：应该自动拼接为 `http://localhost:3000/api/images/goods/goods_1_xxx.jpg`

5. 检查数据库：存储的应该是相对路径 `/api/images/goods/goods_1_xxx.jpg`

### 准备生产环境

1. 编辑 `start-prod.bat` 或 `start-prod.sh`
2. 修改后端环境变量
3. 在 `vue-pet` 目录创建 `.env.production` 文件
4. 配置 `VUE_APP_IMAGE_BASE_URL`
5. 打包前端：`npm run build`
6. 部署到服务器

## 🎊 配置优势总结

✅ **数据独立** - 数据库不依赖域名，迁移更方便  
✅ **灵活切换** - 可以轻松切换CDN、域名  
✅ **环境隔离** - 开发、生产环境完全分离  
✅ **前后端分离** - 前端控制图片域名  
✅ **易于维护** - 修改域名只需改前端环境变量  
✅ **支持多域名** - 可以为不同客户端配置不同域名  

配置已全部完成！现在你可以使用 `start-dev.bat` 启动开发环境进行测试了。
