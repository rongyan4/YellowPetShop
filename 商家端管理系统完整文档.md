# 黄色宠物商城 - 商家端管理系统完整文档

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [数据库设计](#数据库设计)
5. [后端接口文档](#后端接口文档)
6. [前端页面说明](#前端页面说明)
7. [部署指南](#部署指南)
8. [使用说明](#使用说明)
9. [开发规范](#开发规范)

---

## 项目概述

### 系统简介

黄色宠物商城商家端管理系统是一个功能完善的PC端后台管理系统，用于管理商城的商品、会员和订单。系统采用前后端分离架构，与客户端完全独立但共享同一数据库。

### 设计特色

- **薄荷绿主题**：整体采用薄荷绿（#98D8C8）为主色调，清新自然
- **手绘风格图标**：使用SVG手绘风格图标，增加亲和力
- **不对称布局**：登录页面采用左大右小的不对称设计
- **PC端专属**：专为PC端设计，提供更好的管理体验

### 核心功能

1. **商家登录认证**：独立的商家登录系统，使用JWT进行身份验证
2. **商品管理**：商品的增删改查、上下架管理
3. **会员管理**：查看会员信息、启用/禁用会员、删除会员
4. **订单管理**：订单查询、改价、发货、取消等操作
5. **数据概览**：展示商品、会员、订单等关键数据统计

---

## 技术栈

### 后端技术

- **框架**：Spring Boot 3.5.8
- **数据库**：MySQL 8.0+
- **ORM**：MyBatis-Plus 3.5.9
- **认证**：JWT (jjwt 0.11.5)
- **密码加密**：BCrypt
- **Java版本**：JDK 17

### 前端技术

- **框架**：Vue 3.2.13
- **路由**：Vue Router 4.0.3
- **状态管理**：Pinia 2.3.1
- **UI组件**：Vant 4.9.21
- **HTTP客户端**：Axios 1.13.2
- **构建工具**：Vue CLI 5.0

---

## 项目结构

### 后端结构

```
petserver/src/main/java/com/yellow/petshop/
├── controller/                    # 控制器层
│   ├── MerchantController.java           # 商家认证控制器
│   ├── MerchantGoodsController.java      # 商品管理控制器
│   ├── MerchantMemberController.java     # 会员管理控制器
│   └── MerchantOrderController.java      # 订单管理控制器
├── service/                       # 服务层
│   ├── MerchantService.java              # 商家服务
│   ├── MerchantGoodsService.java         # 商品管理服务
│   ├── MerchantMemberService.java        # 会员管理服务
│   └── MerchantOrderService.java         # 订单管理服务
├── mapper/                        # 数据访问层
│   ├── MerchantMapper.java               # 商家Mapper
│   ├── MerchantLoginLogMapper.java       # 登录日志Mapper
│   └── MerchantOperationLogMapper.java   # 操作日志Mapper
└── model/merchant/                # 商家相关实体类
    ├── Merchant.java                     # 商家实体
    ├── MerchantVO.java                   # 商家视图对象
    ├── MerchantLoginRequest.java         # 登录请求DTO
    ├── MerchantLoginLog.java             # 登录日志实体
    └── MerchantOperationLog.java         # 操作日志实体
```

### 前端结构

```
vue-pet/src/
├── views/merchant/                # 商家端页面
│   ├── MerchantLogin.vue                 # 登录页面
│   ├── MerchantLayout.vue                # 主布局页面
│   ├── Dashboard.vue                     # 数据概览页面
│   ├── GoodsManagement.vue               # 商品管理页面
│   ├── MemberManagement.vue              # 会员管理页面
│   └── OrderManagement.vue               # 订单管理页面
├── api/                           # API接口
│   ├── merchant.js                       # 商家认证API
│   ├── merchantGoods.js                  # 商品管理API
│   ├── merchantMember.js                 # 会员管理API
│   └── merchantOrder.js                  # 订单管理API
├── stores/                        # 状态管理
│   └── merchant.js                       # 商家状态管理
└── utils/                         # 工具函数
    └── merchantAuth.js                   # 商家认证工具
```

---

## 数据库设计

### 商家表 (merchant)

```sql
CREATE TABLE `merchant` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '商家ID',
  `username` varchar(50) NOT NULL COMMENT '商家账号',
  `password` varchar(255) NOT NULL COMMENT '密码（BCrypt加密）',
  `merchant_name` varchar(100) NOT NULL COMMENT '商家名称',
  `contact_person` varchar(50) DEFAULT NULL COMMENT '联系人',
  `contact_phone` varchar(20) DEFAULT NULL COMMENT '联系电话',
  `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
  `status` tinyint(1) NOT NULL DEFAULT 1 COMMENT '状态 1-正常 0-禁用',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `last_login_time` datetime DEFAULT NULL COMMENT '最后登录时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家表';
```

### 商家登录日志表 (merchant_login_log)

```sql
CREATE TABLE `merchant_login_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `merchant_id` bigint NOT NULL COMMENT '商家ID',
  `login_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '登录时间',
  `ip_address` varchar(50) DEFAULT NULL COMMENT 'IP地址',
  `user_agent` varchar(255) DEFAULT NULL COMMENT '浏览器信息',
  `login_status` tinyint(1) NOT NULL DEFAULT 1 COMMENT '登录状态 1-成功 0-失败',
  PRIMARY KEY (`id`),
  KEY `idx_merchant_id` (`merchant_id`),
  KEY `idx_login_time` (`login_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家登录日志表';
```

### 商家操作日志表 (merchant_operation_log)

```sql
CREATE TABLE `merchant_operation_log` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '日志ID',
  `merchant_id` bigint NOT NULL COMMENT '商家ID',
  `operation_type` varchar(50) NOT NULL COMMENT '操作类型',
  `operation_desc` varchar(255) NOT NULL COMMENT '操作描述',
  `operation_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  `ip_address` varchar(50) DEFAULT NULL COMMENT 'IP地址',
  PRIMARY KEY (`id`),
  KEY `idx_merchant_id` (`merchant_id`),
  KEY `idx_operation_time` (`operation_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家操作日志表';
```

### 默认账号

- **用户名**：admin
- **密码**：admin123
- **密码加密**：使用BCrypt加密存储

---

## 后端接口文档

### 1. 商家认证接口

#### 1.1 商家登录

**接口地址**：`POST /api/merchant/login`

**请求参数**：
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "登录成功",
  "data": "eyJhbGciOiJIUzI1NiJ9..."
}
```

#### 1.2 获取商家信息

**接口地址**：`GET /api/merchant/info`

**请求头**：
```
Authorization: Bearer {token}
```

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "id": 1,
    "username": "admin",
    "merchantName": "黄色宠物商城",
    "contactPerson": "管理员",
    "contactPhone": "13800138000",
    "email": "admin@yellowpet.com",
    "status": 1,
    "lastLoginTime": "2026-02-19T10:30:00"
  }
}
```

#### 1.3 修改密码

**接口地址**：`POST /api/merchant/password`

**请求参数**：
```json
{
  "oldPassword": "admin123",
  "newPassword": "newpassword123"
}
```

### 2. 商品管理接口

#### 2.1 获取商品列表

**接口地址**：`GET /api/merchant/goods/list`

**请求参数**：
- `page`：页码（默认1）
- `pageSize`：每页数量（默认10）
- `keyword`：搜索关键词（可选）

**响应示例**：
```json
{
  "code": 200,
  "msg": "成功",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "宠物狗粮",
        "price": 99.00,
        "stock": 100,
        "sales": 50,
        "status": 1,
        "image": "/images/goods/ml.png"
      }
    ],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10
  }
}
```

#### 2.2 添加商品

**接口地址**：`POST /api/merchant/goods/add`

**请求参数**：
```json
{
  "name": "宠物玩具",
  "price": 29.90,
  "stock": 200,
  "description": "适合小型犬的玩具"
}
```

#### 2.3 更新商品

**接口地址**：`PUT /api/merchant/goods/update`

**请求参数**：
```json
{
  "id": 1,
  "name": "宠物玩具（更新）",
  "price": 39.90,
  "stock": 150
}
```

#### 2.4 删除商品

**接口地址**：`DELETE /api/merchant/goods/delete/{id}`

#### 2.5 批量删除商品

**接口地址**：`DELETE /api/merchant/goods/batch-delete`

**请求参数**：
```json
[1, 2, 3, 4, 5]
```

### 3. 会员管理接口

#### 3.1 获取会员列表

**接口地址**：`GET /api/merchant/member/list`

**请求参数**：
- `page`：页码
- `pageSize`：每页数量
- `keyword`：搜索关键词（用户名/昵称/手机号）

#### 3.2 更新会员状态

**接口地址**：`PUT /api/merchant/member/status`

**请求参数**：
```json
{
  "userId": 1,
  "status": 0
}
```

#### 3.3 删除会员

**接口地址**：`DELETE /api/merchant/member/delete/{id}`

### 4. 订单管理接口

#### 4.1 获取订单列表

**接口地址**：`GET /api/merchant/order/list`

**请求参数**：
- `page`：页码
- `pageSize`：每页数量
- `status`：订单状态（可选）
- `keyword`：订单号搜索（可选）

#### 4.2 获取订单详情

**接口地址**：`GET /api/merchant/order/detail/{id}`

#### 4.3 修改订单价格

**接口地址**：`PUT /api/merchant/order/price`

**请求参数**：
```json
{
  "orderId": 1,
  "newPrice": 199.00
}
```

**说明**：只能修改未付款订单的价格

#### 4.4 更新物流状态

**接口地址**：`PUT /api/merchant/order/shipping`

**请求参数**：
```json
{
  "orderId": 1,
  "shippingStatus": "shipped",
  "trackingNo": "SF1234567890"
}
```

#### 4.5 取消订单

**接口地址**：`PUT /api/merchant/order/cancel`

**请求参数**：
```json
{
  "orderId": 1,
  "reason": "商家取消"
}
```

---

## 前端页面说明

### 1. 登录页面 (MerchantLogin.vue)

**路由**：`/merchant/login`

**设计特点**：
- 左侧占65%，展示薄荷绿渐变背景和品牌信息
- 右侧占35%，白色背景，登录表单居中偏右
- 支持显示/隐藏密码
- 登录成功后跳转到数据概览页面

**功能**：
- 商家账号密码登录
- 表单验证
- JWT Token存储
- 自动跳转

### 2. 主布局页面 (MerchantLayout.vue)

**路由**：`/merchant`

**设计特点**：
- 左侧固定宽度侧边栏（260px）
- 顶部固定高度导航栏（70px）
- 右侧主内容区域自适应

**功能**：
- 侧边栏导航菜单
- 顶部用户信息展示
- 退出登录功能
- 子路由出口

### 3. 数据概览页面 (Dashboard.vue)

**路由**：`/merchant/dashboard`

**功能**：
- 展示商品总数、会员总数、订单总数、今日订单等统计数据
- 卡片式布局，每个卡片带有渐变色图标
- 欢迎信息和功能介绍

### 4. 商品管理页面 (GoodsManagement.vue)

**路由**：`/merchant/goods`

**功能**：
- 商品列表展示（表格形式）
- 搜索商品（按名称）
- 添加商品（弹窗表单）
- 编辑商品（弹窗表单）
- 删除商品（确认对话框）
- 分页功能

**表格字段**：
- 商品ID
- 商品名称（带图片）
- 分类
- 价格
- 库存
- 销量
- 状态
- 操作按钮

### 5. 会员管理页面 (MemberManagement.vue)

**路由**：`/merchant/member`

**功能**：
- 会员列表展示
- 搜索会员（用户名/昵称/手机号）
- 启用/禁用会员
- 删除会员
- 分页功能

**表格字段**：
- 会员ID
- 用户名
- 昵称
- 手机号
- 性别
- 注册时间
- 状态
- 操作按钮

### 6. 订单管理页面 (OrderManagement.vue)

**路由**：`/merchant/order`

**功能**：
- 订单列表展示
- 按状态筛选订单
- 搜索订单（订单号）
- 查看订单详情
- 修改未付款订单价格
- 订单发货（输入物流单号）
- 取消订单
- 分页功能

**表格字段**：
- 订单号
- 用户ID
- 订单金额
- 订单状态
- 物流状态
- 创建时间
- 操作按钮

**订单状态**：
- pending：待付款
- paid：已付款
- shipped：已发货
- completed：已完成
- cancelled：已取消

---

## 部署指南

### 1. 数据库部署

```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 创建数据库（如果还没有）
CREATE DATABASE pet_shop CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;

# 3. 使用数据库
USE pet_shop;

# 4. 导入商家端表结构
source /path/to/merchant_system.sql;

# 5. 验证表是否创建成功
SHOW TABLES LIKE 'merchant%';
```

### 2. 后端部署

```bash
# 1. 进入后端项目目录
cd petserver

# 2. 配置数据库连接（application.yml）
# 确保数据库URL、用户名、密码正确

# 3. 编译打包
mvn clean package -DskipTests

# 4. 运行
java -jar target/petserver-0.0.1-SNAPSHOT.jar

# 或者使用Maven运行
mvn spring-boot:run
```

### 3. 前端部署

```bash
# 1. 进入前端项目目录
cd vue-pet

# 2. 安装依赖（如果还没有）
npm install

# 3. 开发环境运行
npm run serve

# 4. 生产环境打包
npm run build

# 5. 部署dist目录到Web服务器
```

### 4. 同时启动双端

**Windows系统**：
```bash
# 使用项目根目录的start.bat
start.bat
```

**手动启动**：
```bash
# 终端1：启动后端
cd petserver
mvn spring-boot:run

# 终端2：启动前端
cd vue-pet
npm run serve
```

### 5. 访问地址

- **客户端**：http://localhost:8080
- **商家端**：http://localhost:8080/merchant/login
- **后端API**：http://localhost:3000

---

## 使用说明

### 1. 首次使用

1. 确保数据库已导入商家端表结构
2. 启动后端服务
3. 启动前端服务
4. 访问 http://localhost:8080/merchant/login
5. 使用默认账号登录：
   - 用户名：admin
   - 密码：admin123

### 2. 商品管理

**添加商品**：
1. 点击"添加商品"按钮
2. 填写商品信息（名称、价格、库存、描述）
3. 点击"确定"保存

**编辑商品**：
1. 在商品列表中点击"编辑"按钮
2. 修改商品信息
3. 点击"确定"保存

**删除商品**：
1. 在商品列表中点击"删除"按钮
2. 确认删除操作

**搜索商品**：
1. 在搜索框输入商品名称
2. 按回车键或点击搜索

### 3. 会员管理

**查看会员**：
- 会员列表展示所有注册用户信息

**启用/禁用会员**：
1. 点击"启用"或"禁用"按钮
2. 确认操作

**删除会员**：
1. 点击"删除"按钮
2. 确认删除（此操作不可恢复）

**搜索会员**：
- 支持按用户名、昵称、手机号搜索

### 4. 订单管理

**查看订单**：
- 订单列表展示所有订单信息
- 可按状态筛选订单

**修改订单价格**：
1. 找到待付款订单
2. 点击"改价"按钮
3. 输入新价格
4. 确认修改

**订单发货**：
1. 找到已付款订单
2. 点击"发货"按钮
3. 输入物流单号
4. 确认发货

**取消订单**：
1. 点击"取消"按钮
2. 确认取消操作

### 5. 退出登录

1. 点击左侧边栏底部的"退出登录"按钮
2. 确认退出
3. 自动跳转到登录页面

---

## 开发规范

### 1. 代码规范

**后端**：
- 遵循阿里巴巴Java开发手册
- 使用Lombok简化代码
- 所有接口返回统一的Result对象
- 异常统一处理
- 日志记录关键操作

**前端**：
- 使用Vue 3 Composition API
- 组件命名采用PascalCase
- 文件命名采用PascalCase
- 使用ESLint进行代码检查
- 统一使用Vant组件库

### 2. 命名规范

**数据库**：
- 表名：小写+下划线（如：merchant_login_log）
- 字段名：小写+下划线（如：merchant_id）

**后端**：
- 类名：PascalCase（如：MerchantController）
- 方法名：camelCase（如：getMerchantInfo）
- 常量：大写+下划线（如：MAX_PAGE_SIZE）

**前端**：
- 组件：PascalCase（如：MerchantLogin.vue）
- 方法：camelCase（如：handleLogin）
- 变量：camelCase（如：merchantInfo）

### 3. 注释规范

**后端**：
```java
/**
 * 商家登录
 * @param username 用户名
 * @param password 密码
 * @return JWT Token
 */
public String login(String username, String password) {
    // 实现代码
}
```

**前端**：
```javascript
/**
 * 处理登录
 */
const handleLogin = async () => {
  // 实现代码
};
```

### 4. Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具相关
```

示例：
```
feat: 添加商家端登录功能
fix: 修复订单列表分页问题
docs: 更新商家端使用文档
```

### 5. 安全规范

- 所有密码使用BCrypt加密
- 使用JWT进行身份验证
- 敏感操作记录日志
- 防止SQL注入（使用MyBatis-Plus）
- 前端表单验证
- 后端参数验证

---

## 常见问题

### 1. 登录失败

**问题**：提示"商家账号不存在"或"密码错误"

**解决**：
1. 确认数据库已导入merchant_system.sql
2. 检查默认账号是否存在
3. 确认密码是否正确（admin123）

### 2. 接口请求失败

**问题**：前端请求后端接口返回404或500

**解决**：
1. 确认后端服务已启动
2. 检查后端端口是否为3000
3. 查看后端控制台日志
4. 检查前端代理配置（vue.config.js）

### 3. Token过期

**问题**：操作时提示"登录已过期"

**解决**：
1. 重新登录获取新Token
2. 检查JWT配置的过期时间
3. 确认Token是否正确存储

### 4. 分页数据不显示

**问题**：列表页面没有数据

**解决**：
1. 检查数据库是否有数据
2. 查看浏览器控制台是否有错误
3. 检查后端日志
4. 确认分页参数是否正确

---

## 更新日志

### v1.0.0 (2026-02-19)

**新增功能**：
- ✅ 商家登录认证系统
- ✅ 商品管理（增删改查）
- ✅ 会员管理（查看、启用/禁用、删除）
- ✅ 订单管理（查看、改价、发货、取消）
- ✅ 数据概览页面
- ✅ 操作日志记录
- ✅ 登录日志记录

**设计特色**：
- ✅ 薄荷绿主题
- ✅ 手绘风格SVG图标
- ✅ 不对称登录页面布局
- ✅ 响应式设计

---

## 联系方式

如有问题或建议，请联系：

- **邮箱**：2603726252@qq.com
- **项目地址**：YellowPetShop

---

## 许可证

本项目仅供学习和研究使用。

---

**文档版本**：v1.0.0  
**最后更新**：2026-02-19  
**作者**：rongyan
